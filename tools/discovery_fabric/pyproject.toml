[project]
name = "supremeai-discovery-fabric"
version = "0.1.0"
description = "Evidence-first solution and open-source discovery layer for SupremeAI"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
