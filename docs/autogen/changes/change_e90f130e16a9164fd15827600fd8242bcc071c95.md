# 📋 Commit e90f130e16a9164fd15827600fd8242bcc071c95

## Commit Stats
```
commit e90f130e16a9164fd15827600fd8242bcc071c95
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 17:06:09 2026 +0600

    fix(deploy): add memory and cpu limits to cloud run deployment

 .github/scripts/deploy-backend.py | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

```

## Diff Detail
```diff
commit e90f130e16a9164fd15827600fd8242bcc071c95
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 17:06:09 2026 +0600

    fix(deploy): add memory and cpu limits to cloud run deployment

diff --git a/.github/scripts/deploy-backend.py b/.github/scripts/deploy-backend.py
index 5ecf5b2faf..eb3ae35647 100644
--- a/.github/scripts/deploy-backend.py
+++ b/.github/scripts/deploy-backend.py
@@ -54,7 +54,7 @@ else:
 print("🚀 Deploying new image to Cloud Run...")
 ENCRYPTION_KEY = os.getenv("ENCRYPTION_KEY")
 env_args = f"--set-env-vars ENCRYPTION_KEY='{ENCRYPTION_KEY}'" if ENCRYPTION_KEY else ""
-deploy_cmd = f"gcloud run deploy {SERVICE_NAME} --image {IMAGE} --region {REGION} {env_args} --port 8080 --timeout 300s --quiet"
+deploy_cmd = f"gcloud run deploy {SERVICE_NAME} --image {IMAGE} --region {REGION} {env_args} --port 8080 --memory 2048Mi --cpu 2 --timeout 300s --quiet"
 result = run_cmd(deploy_cmd)
 
 if result.exit_code != 0:

```
