# 📋 Commit 74848276ec43d26928a582f26f89b239c1a124d4

## Commit Stats
```
commit 74848276ec43d26928a582f26f89b239c1a124d4
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 04:10:07 2026 +0600

    style(backend): auto-fix ruff linting errors

 backend/adaptive_engine/test_platform_learner.py             |  4 ++--
 backend/agents/test_legal_agent.py                           |  1 +
 backend/alembic/env.py                                       |  1 +
 .../alembic/versions/ed9761fee64f_create_system_config.py    | 12 +++++++-----
 backend/api/routes/admin_dashboard.py                        |  4 ++--
 backend/api/routes/events.py                                 |  6 +++++-
 backend/api/routes/session_takeover.py                       |  3 ++-
 backend/core/app.py                                          |  7 +++++--
 backend/core/config.py                                       |  3 ++-
 backend/core/config_cache.py                                 |  4 +++-
 backend/core/event_bus.py                                    |  9 ++++++---
 backend/core/lifespan.py                                     |  2 +-
 backend/core/pubsub.py                                       |  4 ++--
 backend/core/redis_manager.py                                |  1 +
 backend/core/self_healer.py                                  |  3 ++-
 backend/middleware/idempotency.py                            |  9 +++++++--
 backend/scripts/trigger_mock_error.py                        |  5 ++++-
 17 files changed, 53 insertions(+), 25 deletions(-)

```

## Diff Detail
```diff
commit 74848276ec43d26928a582f26f89b239c1a124d4
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 04:10:07 2026 +0600

    style(backend): auto-fix ruff linting errors

diff --git a/backend/adaptive_engine/test_platform_learner.py b/backend/adaptive_engine/test_platform_learner.py
index dce2183f2..697c821db 100644
--- a/backend/adaptive_engine/test_platform_learner.py
+++ b/backend/adaptive_engine/test_platform_learner.py
@@ -4,8 +4,8 @@ from unittest.mock import patch
 
 import httpx
 import pytest
-from adaptive_engine.platform_learner import PlatformLearner
 
+from adaptive_engine.platform_learner import PlatformLearner
 from adaptive_engine.registry import PlatformProfile
 
 
@@ -158,5 +158,5 @@ class TestPlatformLearner:
         mock_model_router.async_route_and_generate.side_effect = Exception('Test')
         platform_name = 'test'
         docs_url = 'https://test.com'
-        with pytest.raises(Exception):
+        with pytest.raises(Exception, match='Test'):
             await platform_learner.learn_from_docs(platform_name, docs_url)
diff --git a/backend/agents/test_legal_agent.py b/backend/agents/test_legal_agent.py
index 51c933983..a6f3ff606 100644
--- a/backend/agents/test_legal_agent.py
+++ b/backend/agents/test_legal_agent.py
@@ -2,6 +2,7 @@
 from unittest.mock import patch
 
 import pytest
+
 from agents.legal_agent import LegalAgent
 
 
diff --git a/backend/alembic/env.py b/backend/alembic/env.py
index 969806d94..e68c0f954 100644
--- a/backend/alembic/env.py
+++ b/backend/alembic/env.py
@@ -13,6 +13,7 @@ from alembic import context
 from core.config import settings
 from models.base import Base
 
+
 # Import all models to ensure they are registered with Base.metadata before autogenerate
 
 
diff --git a/backend/alembic/versions/ed9761fee64f_create_system_config.py b/backend/alembic/versions/ed9761fee64f_create_system_config.py
index ab0420904..5bc363ba6 100644
--- a/backend/alembic/versions/ed9761fee64f_create_system_config.py
+++ b/backend/alembic/versions/ed9761fee64f_create_system_config.py
@@ -5,17 +5,19 @@ Revises: 664fe16e33ca
 Create Date: 2026-07-08 02:54:58.952639
 
 """
-from typing import Sequence, Union
+from collections.abc import Sequence
 
-from alembic import op
 import sqlalchemy as sa
 from sqlalchemy.dialects import postgresql
 
+from alembic import op
+
+
 # revision identifiers, used by Alembic.
 revision: str = 'ed9761fee64f'
-down_revision: Union[str, Sequence[str], None] = '664fe16e33ca'
-branch_labels: Union[str, Sequence[str], None] = None
-depends_on: Union[str, Sequence[str], None] = None
+down_revision: str | Sequence[str] | None = '664fe16e33ca'
+branch_labels: str | Sequence[str] | None = None
+depends_on: str | Sequence[str] | None = None
 
 
 def upgrade() -> None:
diff --git a/backend/api/routes/admin_dashboard.py b/backend/api/routes/admin_dashboard.py
index 1c05979a6..a559a1beb 100644
--- a/backend/api/routes/admin_dashboard.py
+++ b/backend/api/routes/admin_dashboard.py
@@ -64,14 +64,14 @@ def require_admin_token(credentials: HTTPAuthorizationCredentials = Depends(secu
                 logger.warning("Redis not configured; falling back to in-memory JWT blacklist check.")
 
         return decoded
-    except Exception:  # noqa: BLE001
+    except Exception as err:  # noqa: BLE001
         logger.warning("Admin token validation failed", exc_info=True)
         expected = os.getenv("SUPREMEAI_API_TOKEN") or ""
         if expected and secrets.compare_digest(token, expected):
             return {"uid": "admin", "role": "admin"}
         raise HTTPException(
             status_code=401, detail="Authentication failed."
-        )
+        ) from err
 
 
 def admin_rate_limit(request: Request):
diff --git a/backend/api/routes/events.py b/backend/api/routes/events.py
index 75d2dd821..c6f676618 100644
--- a/backend/api/routes/events.py
+++ b/backend/api/routes/events.py
@@ -1,9 +1,13 @@
 import asyncio
 import json
-from fastapi import APIRouter, Request
+
+from fastapi import APIRouter
+from fastapi import Request
 from sse_starlette.sse import EventSourceResponse
+
 from core.pubsub import global_pubsub
 
+
 router = APIRouter(tags=["Events"])
 
 @router.get("/dashboard/stream")
diff --git a/backend/api/routes/session_takeover.py b/backend/api/routes/session_takeover.py
index 23705b3ae..a69b2f485 100644
--- a/backend/api/routes/session_takeover.py
+++ b/backend/api/routes/session_takeover.py
@@ -47,7 +47,8 @@ async def mock_screencast_emitter(websocket: WebSocket, session_id: str):
         raise
     except Exception as e:  # noqa: BLE001
         logger.exception(f"❌ Critical task failure in session_takeover.py: {e}")
-        from core.event_bus import error_event_bus, ErrorEvent
+        from core.event_bus import ErrorEvent
+        from core.event_bus import error_event_bus
         await error_event_bus.emit_async(
             ErrorEvent(
                 module="backend.api.routes.session_takeover",
diff --git a/backend/core/app.py b/backend/core/app.py
index 4344a57f6..f4de9e085 100644
--- a/backend/core/app.py
+++ b/backend/core/app.py
@@ -1,8 +1,8 @@
 import logging
 import os
 import secrets
-import sentry_sdk
 
+import sentry_sdk
 from fastapi import Depends
 from fastapi import FastAPI
 from fastapi import HTTPException
@@ -455,7 +455,10 @@ app.router.lifespan_context = lifespan.app_lifespan
 def router_health_check(fastapi_app: FastAPI):
     expected_count = 20
     if len(fastapi_app.routes) < expected_count:
-        logger.critical(f"🔥 CRITICAL: Only {len(fastapi_app.routes)} routes loaded. Expected at least {expected_count}. Some routers failed to load silently!")
+        logger.critical(
+            f"🔥 CRITICAL: Only {len(fastapi_app.routes)} routes loaded. "
+            f"Expected at least {expected_count}. Some routers failed to load silently!"
+        )
 
 router_health_check(app)
 
diff --git a/backend/core/config.py b/backend/core/config.py
index 76911469b..574d96486 100644
--- a/backend/core/config.py
+++ b/backend/core/config.py
@@ -4,7 +4,8 @@ from pathlib import Path
 
 from dotenv import load_dotenv
 from loguru import logger
-from pydantic import Field, PrivateAttr
+from pydantic import Field
+from pydantic import PrivateAttr
 from pydantic import ValidationInfo
 from pydantic import computed_field
 from pydantic import field_validator
diff --git a/backend/core/config_cache.py b/backend/core/config_cache.py
index a44361fea..d75f5260b 100644
--- a/backend/core/config_cache.py
+++ b/backend/core/config_cache.py
@@ -112,7 +112,8 @@ class ConfigCache:
                 logger.info(f"ConfigCache: Loaded {len(configs)} configs from DB")
             except RuntimeError as e:
                 logger.exception(f"❌ Critical task failure in config_cache.py: {e}")
-                from core.event_bus import error_event_bus, ErrorEvent
+                from core.event_bus import ErrorEvent
+                from core.event_bus import error_event_bus
                 error_event_bus.emit(
                     ErrorEvent(
                         module="backend.core.config_cache",
@@ -139,6 +140,7 @@ class ConfigCache:
     async def refresh_async(self):
         """Asynchronous refresh, mainly for startup."""
         from sqlalchemy import select
+
         from database.session import AsyncSessionLocal
         from models.system_config import SystemConfig
 
diff --git a/backend/core/event_bus.py b/backend/core/event_bus.py
index c9cc6cb1f..af8daf45c 100644
--- a/backend/core/event_bus.py
+++ b/backend/core/event_bus.py
@@ -1,7 +1,10 @@
 import asyncio
 import logging
+from collections.abc import Callable
+from typing import Any
+
 from pydantic import BaseModel
-from typing import Dict, Any, Callable, List
+
 
 logger = logging.getLogger("supremeai.event_bus")
 
@@ -10,11 +13,11 @@ class ErrorEvent(BaseModel):
     error_type: str
     message: str
     severity: str  # CRITICAL, WARNING, INFO
-    context: Dict[str, Any]
+    context: dict[str, Any]
 
 class ErrorEventBus:
     def __init__(self):
-        self._listeners: List[Callable[[ErrorEvent], asyncio.Future]] = []
+        self._listeners: list[Callable[[ErrorEvent], asyncio.Future]] = []
 
     def register_listener(self, listener: Callable[[ErrorEvent], asyncio.Future]):
         self._listeners.append(listener)
diff --git a/backend/core/lifespan.py b/backend/core/lifespan.py
index 3cfe98a6e..1ddad38fc 100644
--- a/backend/core/lifespan.py
+++ b/backend/core/lifespan.py
@@ -7,12 +7,12 @@ from loguru import logger
 
 from core import services
 from core.config import settings
+from core.config_cache import config_cache
 from core.discord_bot import SupremeDiscordBot
 from core.orchestrator import Orchestrator
 from core.pgbouncer_pool import get_db_pool
 from core.pgbouncer_pool import init_db_pool
 from core.redis_manager import redis_manager
-from core.config_cache import config_cache
 
 
 async def _ensure_api_key_tables() -> None:
diff --git a/backend/core/pubsub.py b/backend/core/pubsub.py
index 9f029dc4e..75faa863f 100644
--- a/backend/core/pubsub.py
+++ b/backend/core/pubsub.py
@@ -1,9 +1,9 @@
 import asyncio
-from typing import Dict, Set
+
 
 class PubSub:
     def __init__(self):
-        self.subscribers: Dict[str, Set[asyncio.Queue]] = {}
+        self.subscribers: dict[str, set[asyncio.Queue]] = {}
 
     def subscribe(self, channel: str) -> asyncio.Queue:
         if channel not in self.subscribers:
diff --git a/backend/core/redis_manager.py b/backend/core/redis_manager.py
index c38352dd9..e766fa1c4 100644
--- a/backend/core/redis_manager.py
+++ b/backend/core/redis_manager.py
@@ -2,6 +2,7 @@
 # রেডিস ডাউন থাকলে এটি কোনো সিকিউরিটি গেট বাইপাস করতে দেবে না (Fail-Closed)।
 
 import time
+
 import redis.asyncio as aioredis
 
 from core.config import settings
diff --git a/backend/core/self_healer.py b/backend/core/self_healer.py
index 89a6966d3..2a8e1b599 100644
--- a/backend/core/self_healer.py
+++ b/backend/core/self_healer.py
@@ -5,7 +5,8 @@ from typing import Any
 
 from loguru import logger
 
-from core.event_bus import error_event_bus, ErrorEvent
+from core.event_bus import ErrorEvent
+from core.event_bus import error_event_bus
 
 
 class SelfHealerService:
diff --git a/backend/middleware/idempotency.py b/backend/middleware/idempotency.py
index 0600342d2..378224f5f 100644
--- a/backend/middleware/idempotency.py
+++ b/backend/middleware/idempotency.py
@@ -1,10 +1,12 @@
 import json
 
-from fastapi import HTTPException, Request
+from fastapi import HTTPException
+from fastapi import Request
 from fastapi.responses import JSONResponse
 from loguru import logger
 from starlette.middleware.base import BaseHTTPMiddleware
 
+
 # বাংলা মন্তব্য: Redis-based Distributed Idempotency Middleware
 # পূর্বে Firestore (Firebase) ব্যবহার করা হতো, যা Serverless-এ ব্যয়বহুল এবং ধীর ছিল।
 # এখন Redis SET NX (atomic, sub-millisecond) ব্যবহার করা হচ্ছে — fail-open মোডে।
@@ -41,7 +43,10 @@ class IdempotencyMiddleware(BaseHTTPMiddleware):
 
         # বাংলা মন্তব্য: Redis lock অধিগ্রহণের চেষ্টা (SET NX — atomic)
         try:
-            from core.redis_manager import acquire_idempotency_lock, release_idempotency_lock, cache_response_and_release_lock, redis_manager
+            from core.redis_manager import acquire_idempotency_lock
+            from core.redis_manager import cache_response_and_release_lock
+            from core.redis_manager import redis_manager
+            from core.redis_manager import release_idempotency_lock
         except ImportError:
             # Redis ইমপোর্ট ব্যর্থ হলে fail-open — request পাস করে দাও
             logger.warning("[Idempotency] Failed to import redis_manager — skipping check (fail-open)")
diff --git a/backend/scripts/trigger_mock_error.py b/backend/scripts/trigger_mock_error.py
index a79cffdda..9bfb6597b 100644
--- a/backend/scripts/trigger_mock_error.py
+++ b/backend/scripts/trigger_mock_error.py
@@ -1,10 +1,13 @@
 import asyncio
 import logging
 
+
 # Configure logger to output to terminal
 logging.basicConfig(level=logging.WARNING)
 
-from core.event_bus import error_event_bus, ErrorEvent  # noqa: E402
+from core.event_bus import ErrorEvent  # noqa: E402
+from core.event_bus import error_event_bus  # noqa: E402
+
 
 async def main():
     print("Mocking an error trigger...")  # noqa: T201

```
