# 📋 Commit 79b1b406d1a067e6245dabbe2eaf41b5e350b7d2

## Commit Stats
```
commit 79b1b406d1a067e6245dabbe2eaf41b5e350b7d2
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 16:22:44 2026 +0600

    fix(backend): fix sqlite3 db initialization OperationalError and update AuthMiddleware import

 backend/adaptive_engine/experience_db.py | 4 +++-
 backend/admin/god.py                     | 2 +-
 backend/core/app.py                      | 4 ++--
 backend/memory/sqlite_store.py           | 2 +-
 4 files changed, 7 insertions(+), 5 deletions(-)

```

## Diff Detail
```diff
commit 79b1b406d1a067e6245dabbe2eaf41b5e350b7d2
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 16:22:44 2026 +0600

    fix(backend): fix sqlite3 db initialization OperationalError and update AuthMiddleware import

diff --git a/backend/adaptive_engine/experience_db.py b/backend/adaptive_engine/experience_db.py
index 233b761933..6987bc2941 100644
--- a/backend/adaptive_engine/experience_db.py
+++ b/backend/adaptive_engine/experience_db.py
@@ -34,8 +34,10 @@ class ExperienceDatabase:
     def __init__(self, db_path: str = None):
         import os
 
-        if db_path is None:
+        if not db_path:
             db_path = os.getenv("EXPERIENCE_DB_PATH", "data/experience.db")
+        if not db_path:
+            db_path = "data/experience.db"
         self.db_path = Path(db_path)
         if str(self.db_path) != ":memory:":
             self.db_path.parent.mkdir(parents=True, exist_ok=True)
diff --git a/backend/admin/god.py b/backend/admin/god.py
index 54bf29be1f..be1337e0ba 100644
--- a/backend/admin/god.py
+++ b/backend/admin/god.py
@@ -17,7 +17,7 @@ class AdminGodLayer:
     """
 
     def __init__(self, db_path: str = None):
-        if db_path is None:
+        if not db_path:
             # বাংলা মন্তব্য: settings থেকে রুলস ডাটাবেস পাথ রিড করা হচ্ছে
             try:
                 from core.config import settings
diff --git a/backend/core/app.py b/backend/core/app.py
index 269a33449b..9d19c419a5 100644
--- a/backend/core/app.py
+++ b/backend/core/app.py
@@ -28,7 +28,7 @@ from core.observability_middleware import ObservabilityMiddleware
 from core.rate_limiter import RateLimitMiddleware
 
 # বাংলা মন্তব্য: unused import setup_tracing সরানো হলো (এটি lifespan-এ শিফট করা হয়েছে)
-from middleware.auth_middleware import ZeroTrustAuthMiddleware
+from core.auth_middleware import AuthMiddleware
 from middleware.idempotency import IdempotencyMiddleware
 
 
@@ -125,7 +125,7 @@ app.add_middleware(TrustedOriginMiddleware)
 app.add_middleware(ChaosInjectorMiddleware)
 app.add_middleware(ObservabilityMiddleware)
 app.add_middleware(HoneypotMiddleware)
-app.add_middleware(ZeroTrustAuthMiddleware)
+app.add_middleware(AuthMiddleware)
 app.add_middleware(RateLimitMiddleware, requests_per_minute=120, burst=20)
 app.add_middleware(IdempotencyMiddleware)
 app.add_middleware(APIKeyAuthMiddleware)
diff --git a/backend/memory/sqlite_store.py b/backend/memory/sqlite_store.py
index 4d1414a99e..95e9426a6c 100644
--- a/backend/memory/sqlite_store.py
+++ b/backend/memory/sqlite_store.py
@@ -7,7 +7,7 @@ class SQLiteMemoryStore:
     """Manages transactional data, session variables, and task history in SQLite database."""
 
     def __init__(self, db_path: str = None):
-        if db_path is None:
+        if not db_path:
             base_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
             self.db_path = os.path.join(base_dir, "data", "supreme_memory.db")
         else:

```
