# 📋 Commit e6c383a6a19df6945869b52139ba53c172a408e8

## Commit Stats
```
commit e6c383a6a19df6945869b52139ba53c172a408e8
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 04:19:16 2026 +0600

    ci: merge ai architecture generation into codebase docs job

 .github/workflows/supreme-core-ci.yml | 55 ++++++++++++-----------------------
 1 file changed, 18 insertions(+), 37 deletions(-)

```

## Diff Detail
```diff
commit e6c383a6a19df6945869b52139ba53c172a408e8
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 04:19:16 2026 +0600

    ci: merge ai architecture generation into codebase docs job

diff --git a/.github/workflows/supreme-core-ci.yml b/.github/workflows/supreme-core-ci.yml
index 024670011..23c383e49 100644
--- a/.github/workflows/supreme-core-ci.yml
+++ b/.github/workflows/supreme-core-ci.yml
@@ -98,39 +98,7 @@ jobs:
           fi
           echo "✅ PASS: All httpx clients have explicit timeouts" >> $GITHUB_STEP_SUMMARY
 
-  ai-architecture-generation:
-    name: 🤖 AI Architecture Documentation
-    needs: [pre-merge-gate]
-    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
-    runs-on: ubuntu-latest
-    permissions:
-      contents: write
-    steps:
-      - uses: actions/checkout@v4
-        with:
-          fetch-depth: 0
-      - name: Set up Python
-        uses: actions/setup-python@v5
-        with:
-          python-version: ${{ env.PYTHON_VERSION }}
-      - name: Install dependencies
-        run: |
-          pip install poetry litellm pyyaml httpx
-          cd backend && poetry install --sync --with dev --without ml,tools
-      - name: Generate AI Docs
-        env:
-          SUPREMEAI_API_KEY: ${{ secrets.SUPREMEAI_API_KEY }}
-          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
-        run: |
-          cd backend && poetry run python ../scripts/generate_openapi.py
-          cd .. && python scripts/auto_generate_architecture_docs.py
-      - name: Auto-Commit Docs
-        run: |
-          git config user.name "SupremeAI-DocBot"
-          git config user.email "docbot@supremeai.dev"
-          git add ADR-*.md DIAGRAM-*.md THREAT-MODEL-*.md API-swagger.yaml || true
-          git commit -m "docs: [auto-docs] Automated ADR/DFD/Threat Model updates [skip ci]" || exit 0
-          git push
+
 
   generate-pr-summary:
     name: 📝 Generate PR Diff Summary
@@ -827,16 +795,29 @@ jobs:
         uses: actions/setup-python@v5
         with:
           python-version: '3.11'
+      - name: Install dependencies
+        run: |
+          pip install poetry litellm pyyaml httpx
+          cd backend && poetry install --sync --with dev --without ml,tools
+          
+      - name: 🤖 Generate AI Architecture Docs (ADR, DFD, OpenAPI)
+        env:
+          SUPREMEAI_API_KEY: ${{ secrets.SUPREMEAI_API_KEY }}
+          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
+        run: |
+          cd backend && poetry run python ../scripts/generate_openapi.py
+          cd .. && python scripts/auto_generate_architecture_docs.py
+
       - name: Generate Smart Docs & Dashboard
         run: python scripts/generate_smart_docs.py
         
       - name: Commit and Push Docs to Repo
         id: push_docs
         run: |
-          git config --global user.name "github-actions[bot]"
-          git config --global user.email "github-actions[bot]@users.noreply.github.com"
-          git add -f docs/autogen/
-          git diff-index --quiet HEAD || (git commit -m "docs: auto-update codebase docs & dashboard [skip ci]" && git push) || echo "No changes to commit"
+          git config --global user.name "SupremeAI-DocBot"
+          git config --global user.email "docbot@supremeai.dev"
+          git add -f docs/autogen/ ADR-*.md DIAGRAM-*.md THREAT-MODEL-*.md API-swagger.yaml || true
+          git diff-index --quiet HEAD || (git commit -m "docs: [auto-docs] Automated ADR, Codebase Docs & Dashboard [skip ci]" && git push) || echo "No changes to commit"
       - name: Force Purge Unnecessary Old Caches (Smartest Clean)
         if: always()
         env:

```
