# 📋 Commit c7140d035f4524b389b5f4e069f91ea2dac2c568

## Commit Stats
```
commit c7140d035f4524b389b5f4e069f91ea2dac2c568
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 00:48:00 2026 +0600

    feat: Separate Admin and User dashboards logically with isolated build targets

 apps/studio-client/package.json                    |  4 ++++
 .../src/components/admin/AdminLogin.tsx            | 24 +++++++++++++---------
 apps/studio-client/vite.config.ts                  |  2 ++
 firebase.json                                      |  2 +-
 package.json                                       |  3 ++-
 pnpm-lock.yaml                                     |  3 +++
 vercel.json                                        |  5 +++--
 7 files changed, 29 insertions(+), 14 deletions(-)

```

## Diff Detail
```diff
commit c7140d035f4524b389b5f4e069f91ea2dac2c568
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 00:48:00 2026 +0600

    feat: Separate Admin and User dashboards logically with isolated build targets

diff --git a/apps/studio-client/package.json b/apps/studio-client/package.json
index 535adecad8..0921735402 100644
--- a/apps/studio-client/package.json
+++ b/apps/studio-client/package.json
@@ -9,7 +9,11 @@
   "main": "main.js",
   "scripts": {
     "dev": "vite",
+    "dev:admin": "cross-env VITE_PORTAL_TYPE=admin vite",
+    "dev:user": "cross-env VITE_PORTAL_TYPE=user vite",
     "build": "vite build",
+    "build:admin": "cross-env VITE_PORTAL_TYPE=admin vite build",
+    "build:user": "cross-env VITE_PORTAL_TYPE=user vite build",
     "build:report": "vite build --mode production --reporter=json",
     "lint": "eslint .",
     "preview": "vite preview",
diff --git a/apps/studio-client/src/components/admin/AdminLogin.tsx b/apps/studio-client/src/components/admin/AdminLogin.tsx
index 91f6fa3bc3..a73d13f63f 100644
--- a/apps/studio-client/src/components/admin/AdminLogin.tsx
+++ b/apps/studio-client/src/components/admin/AdminLogin.tsx
@@ -31,13 +31,18 @@ export function LoginView({
           <p className="text-slate-400 text-xs mt-1">Authorized access only. Authentication protocol required.</p>
         </div>
         
-        <div className="flex flex-col gap-3.5">
+        <form 
+          className="flex flex-col gap-3.5"
+          onSubmit={(e) => {
+            e.preventDefault();
+            handleAdminLogin();
+          }}
+        >
           <input
             type="password"
             placeholder="Enter Authentication Code..."
             value={adminPassword}
             onChange={e => setAdminPassword(e.target.value)}
-            onKeyDown={e => e.key === 'Enter' && handleAdminLogin()}
             className="w-full text-center bg-[#07090f] border border-slate-800 rounded-xl px-4 py-3 text-sm text-white focus:outline-none focus:border-[#00f3ff] transition-all font-mono tracking-widest"
           />
           {otpRequired && (
@@ -46,19 +51,18 @@ export function LoginView({
               placeholder="Enter 6-digit OTP"
               value={adminOtp}
               onChange={e => setAdminOtp(e.target.value)}
-              onKeyDown={e => e.key === 'Enter' && handleAdminLogin()}
               className="w-full text-center bg-[#07090f] border border-slate-800 rounded-xl px-4 py-3 text-sm text-white focus:outline-none focus:border-[#00f3ff] transition-all font-mono tracking-widest"
             />
           )}
           {adminError && <div className="text-[#ff4d4f] text-xs mt-1 font-mono">{adminError}</div>}
-        </div>
         
-        <button
-          onClick={handleAdminLogin}
-          className="cyber-button w-full uppercase py-3 text-xs tracking-wider font-mono font-bold"
-        >
-          {otpRequired ? 'Verify OTP' : 'Authorize Access'}
-        </button>
+          <button
+            type="submit"
+            className="cyber-button w-full uppercase py-3 text-xs tracking-wider font-mono font-bold mt-2"
+          >
+            {otpRequired ? 'Verify OTP' : 'Authorize Access'}
+          </button>
+        </form>
       </div>
     </div>
   );
diff --git a/apps/studio-client/vite.config.ts b/apps/studio-client/vite.config.ts
index 2183f3302a..3bea3da178 100644
--- a/apps/studio-client/vite.config.ts
+++ b/apps/studio-client/vite.config.ts
@@ -45,6 +45,8 @@ export default defineConfig({
     }
   },
   build: {
+    outDir: process.env.VITE_PORTAL_TYPE === 'admin' ? 'dist-admin' : 'dist-user',
+    emptyOutDir: true,
     rollupOptions: {
       output: {
         manualChunks: {
diff --git a/firebase.json b/firebase.json
index 99edd0158f..b4258e1a89 100644
--- a/firebase.json
+++ b/firebase.json
@@ -104,7 +104,7 @@
     },
     {
       "target": "admin",
-      "public": "apps/studio-client/dist",
+      "public": "apps/studio-client/dist-admin",
       "ignore": [
         "firebase.json",
         "**/.*",
diff --git a/package.json b/package.json
index 920e070382..63017112f5 100644
--- a/package.json
+++ b/package.json
@@ -46,6 +46,7 @@
     "pnpm": ">=9.0.0"
   },
   "dependencies": {
-    "@webcontainer/api": "^1.6.4"
+    "@webcontainer/api": "^1.6.4",
+    "rollup": "^4.62.2"
   }
 }
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 42b8be1e0b..c1706be75a 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -11,6 +11,9 @@ importers:
       '@webcontainer/api':
         specifier: ^1.6.4
         version: 1.6.4
+      rollup:
+        specifier: ^4.62.2
+        version: 4.62.2
     devDependencies:
       '@axe-core/playwright':
         specifier: ^4.12.1
diff --git a/vercel.json b/vercel.json
index 6e667a1377..c1180b572c 100644
--- a/vercel.json
+++ b/vercel.json
@@ -1,7 +1,8 @@
 {
-  "buildCommand": "pnpm turbo run build --filter=supremeai-studio-client",
+  "version": 2,
+  "buildCommand": "pnpm --filter supremeai-studio-client build:user",
   "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/studio-client",
-  "outputDirectory": "apps/studio-client/dist",
+  "outputDirectory": "apps/studio-client/dist-user",
   "framework": "vite",
   "env": {
     "VITE_PORTAL_TYPE": "user"

```
