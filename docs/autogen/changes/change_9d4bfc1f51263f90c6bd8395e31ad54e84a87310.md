# 📋 Commit 9d4bfc1f51263f90c6bd8395e31ad54e84a87310

## Commit Stats
```
commit 9d4bfc1f51263f90c6bd8395e31ad54e84a87310
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 21:45:57 2026 +0600

    chore: commit all changes

 .github/workflows/deploy.yml                 |  2 +-
 .github/workflows/supreme-release-builds.yml | 19 ++++++++++++++-----
 .github/workflows/sync-from-prod.yml         |  5 ++---
 apps/mobile/android/app/build.gradle.kts     |  2 +-
 scripts/commit_supreme_ci.yml                |  4 +++-
 5 files changed, 21 insertions(+), 11 deletions(-)

```

## Diff Detail
```diff
commit 9d4bfc1f51263f90c6bd8395e31ad54e84a87310
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 21:45:57 2026 +0600

    chore: commit all changes

diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 95fcbe96a..2b0ba991a 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -20,7 +20,7 @@ jobs:
 
     steps:
       - name: 📥 Checkout Code
-        uses: actions/checkout@v7
+        uses: actions/checkout@v4
 
       - name: 🔐 Google Cloud Auth
         uses: google-github-actions/auth@v3
diff --git a/.github/workflows/supreme-release-builds.yml b/.github/workflows/supreme-release-builds.yml
index 75e43a335..d38992b4f 100644
--- a/.github/workflows/supreme-release-builds.yml
+++ b/.github/workflows/supreme-release-builds.yml
@@ -48,20 +48,29 @@ jobs:
           flutter-version: ${{ env.FLUTTER_VERSION }}
           cache: true
           
-      - name: Build APK
+      - name: Cache Gradle Packages
+        if: matrix.target == 'APK'
+        uses: actions/cache@v4
+        with:
+          path: ~/.gradle/caches
+          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
+          restore-keys: |
+            ${{ runner.os }}-gradle-
+
+      - name: Build AppBundle
         if: matrix.target == 'APK'
         working-directory: apps/mobile
         run: |
           flutter pub get
 
-          flutter build apk --release --target-platform android-arm64
+          flutter build appbundle --release --target-platform android-arm64
           
-      - name: Upload APK Artifact
+      - name: Upload AppBundle Artifact
         if: matrix.target == 'APK'
         uses: actions/upload-artifact@v4
         with:
-          name: supremeai-mobile-apk
-          path: apps/mobile/build/app/outputs/flutter-apk/*release*.apk
+          name: supremeai-mobile-appbundle
+          path: apps/mobile/build/app/outputs/bundle/release/*.aab
           retention-days: 7
 
       # ----------------------------------------------------
diff --git a/.github/workflows/sync-from-prod.yml b/.github/workflows/sync-from-prod.yml
index db8caa2d8..fa22ac8b3 100644
--- a/.github/workflows/sync-from-prod.yml
+++ b/.github/workflows/sync-from-prod.yml
@@ -17,10 +17,9 @@ jobs:
     if: "contains(github.event.head_commit.message, '[CI-SYNC]') == false"
 
     steps:
-      - name:  checkout staging repo
-        uses: actions/checkout@v7
+      - name: checkout staging repo
+        uses: actions/checkout@v4
         with:
-          token: ${{ secrets.STAGING_REPO_TOKEN }} # Use a PAT with write access
           ref: ${{ github.ref_name }}
 
       - name: Configure Git
diff --git a/apps/mobile/android/app/build.gradle.kts b/apps/mobile/android/app/build.gradle.kts
index ea1d17ade..3541c6425 100644
--- a/apps/mobile/android/app/build.gradle.kts
+++ b/apps/mobile/android/app/build.gradle.kts
@@ -23,7 +23,7 @@ android {
 
     defaultConfig {
         applicationId = "com.example.supremeai"
-        minSdk = flutter.minSdkVersion
+        minSdk = 23 // বাংলা মন্তব্য: firebase_app_check এর জন্য minSdkVersion 23 এ উন্নীত করা হলো
         targetSdk = flutter.targetSdkVersion
         versionCode = flutter.versionCode
         versionName = flutter.versionName
diff --git a/scripts/commit_supreme_ci.yml b/scripts/commit_supreme_ci.yml
index 2c800ff27..b8cb3f927 100644
--- a/scripts/commit_supreme_ci.yml
+++ b/scripts/commit_supreme_ci.yml
@@ -1429,7 +1429,9 @@ jobs:
             --service=supremeai-api \
             --region=${{ secrets.GCP_REGION }} \
             --format="value(name)" \
-            --sort-by="~createTime" | tail -n +6 | \
+            --sort-by="~createTime" | \
+            grep -v "$(gcloud run services describe supremeai-api --region=${{ secrets.GCP_REGION }} --format='value(traffic.revisionName)')" | \
+            tail -n +6 | \
             xargs -r -I {} gcloud run revisions delete {} \
               --region=${{ secrets.GCP_REGION }} --quiet
           echo "✅ Cloud Run revision cleanup done"

```
