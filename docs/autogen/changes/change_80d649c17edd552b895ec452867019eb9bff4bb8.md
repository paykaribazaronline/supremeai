# 📋 Commit 80d649c17edd552b895ec452867019eb9bff4bb8

## Commit Stats
```
commit 80d649c17edd552b895ec452867019eb9bff4bb8
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 16:05:54 2026 +0600

    fix(core): prevent startup crash on config load failure and fix actions checkout credentials

 .github/workflows/supreme-core-ci.yml | 1 +
 backend/core/lifespan.py              | 9 ++++++---
 2 files changed, 7 insertions(+), 3 deletions(-)

```

## Diff Detail
```diff
commit 80d649c17edd552b895ec452867019eb9bff4bb8
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 16:05:54 2026 +0600

    fix(core): prevent startup crash on config load failure and fix actions checkout credentials

diff --git a/.github/workflows/supreme-core-ci.yml b/.github/workflows/supreme-core-ci.yml
index a8b26f82f..03605d8d3 100644
--- a/.github/workflows/supreme-core-ci.yml
+++ b/.github/workflows/supreme-core-ci.yml
@@ -732,6 +732,7 @@ jobs:
       - uses: actions/checkout@v4
         with:
           fetch-depth: 0
+          persist-credentials: false
       
       - name: 📤 Sync to Secondary Repo (Staging Dispatch)
         if: env.MIRROR_REPO_TOKEN != ''
diff --git a/backend/core/lifespan.py b/backend/core/lifespan.py
index 292def983..cdabff678 100644
--- a/backend/core/lifespan.py
+++ b/backend/core/lifespan.py
@@ -112,10 +112,13 @@ async def app_lifespan(app):
 
     try:
         await config_cache.refresh_async()
+        logger.info("✅ System configuration cache successfully initialized.")
     except Exception as exc:  # noqa: BLE001
-        logger.critical(f"🚨 সিস্টেম স্টার্টআপ ব্লক করা হয়েছে - কনফিগারেশন অনুপস্থিত! {exc}")
-        import sys
-        sys.exit(1)
+        # প্রোডাকশনে ডাটাবেজ সাময়িক ডাউন থাকলেও সার্ভার যেন বুট হতে পারে
+        logger.warning(f"⚠️ Async config load failed, falling back to local DEFAULT_CONFIGS: {exc}")
+        from core.config_cache import DEFAULT_CONFIGS
+        config_cache._cache = dict(DEFAULT_CONFIGS)
+        # sys.exit(1) রিমুভ করা হলো যাতে ক্লাউড রান হেলথ চেক পাস করতে পারে
 
     try:
         await redis_manager.initialize()

```
