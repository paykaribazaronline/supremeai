# 📋 Commit 58fb2050b43090b0f18bb49742b2bb564ec74a55

## Commit Stats
```
commit 58fb2050b43090b0f18bb49742b2bb564ec74a55
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 20:57:24 2026 +0600

    chore(ci): consolidate github pages docs deployment into supreme-core-ci

 .github/workflows/deploy-docs.yml     | 37 -----------------------------------
 .github/workflows/supreme-core-ci.yml | 35 ++++++++++++++++++++++++++-------
 2 files changed, 28 insertions(+), 44 deletions(-)

```

## Diff Detail
```diff
commit 58fb2050b43090b0f18bb49742b2bb564ec74a55
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 20:57:24 2026 +0600

    chore(ci): consolidate github pages docs deployment into supreme-core-ci

diff --git a/.github/workflows/deploy-docs.yml b/.github/workflows/deploy-docs.yml
deleted file mode 100644
index 07f6f05a4..000000000
--- a/.github/workflows/deploy-docs.yml
+++ /dev/null
@@ -1,37 +0,0 @@
-name: 🚀 Deploy Docs to GitHub Pages
-
-on:
-  push:
-    branches: [main]
-    paths: ['docs/autogen/**'] # শুধুমাত্র অটো-জেনারেটেড ডক্স আপডেট হলে রান হবে
-
-permissions:
-  contents: read
-  pages: write
-  id-token: write
-
-jobs:
-  deploy:
-    environment:
-      name: github-pages
-      url: ${{ steps.deployment.outputs.page_url }}
-    runs-on: ubuntu-latest
-    steps:
-      - uses: actions/checkout@v4
-      
-      - name: Build Dashboard Site
-        run: |
-          mkdir -p public
-          cp -r docs/autogen/* public/
-          
-      - name: Setup Pages
-        uses: actions/configure-pages@v5
-        
-      - name: Upload artifact
-        uses: actions/upload-pages-artifact@v3
-        with:
-          path: 'public'
-          
-      - name: Deploy to GitHub Pages
-        id: deployment
-        uses: actions/deploy-pages@v4
diff --git a/.github/workflows/supreme-core-ci.yml b/.github/workflows/supreme-core-ci.yml
index 7254879e6..b1654a744 100644
--- a/.github/workflows/supreme-core-ci.yml
+++ b/.github/workflows/supreme-core-ci.yml
@@ -499,12 +499,19 @@ jobs:
           git push mirror main:refs/heads/main
 
   generate-codebase-docs:
-    name: 📝 Auto-Generate Codebase Docs
-    # বাংলা মন্তব্য: মেইন বা ডেভেলপ ব্রাঞ্চে পুশ করা হলে স্বয়ংক্রিয়ভাবে কোডবেসের মার্কডাউন ফাইল জেনারেট হয়ে পুশ হবে
+    name: 📝 Auto-Generate & Deploy Docs
+    # বাংলা মন্তব্য: মেইন বা ডেভেলপ ব্রাঞ্চে পুশ করা হলে স্বয়ংক্রিয়ভাবে কোডবেসের মার্কডাউন ফাইল ও ড্যাশবোর্ড জেনারেট এবং ডিপ্লয় হবে
     runs-on: ubuntu-latest
     if: |
       github.event_name == 'push' &&
       (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
+    permissions:
+      contents: write
+      pages: write
+      id-token: write
+    environment:
+      name: github-pages
+      url: ${{ steps.deployment.outputs.page_url }}
     steps:
       - uses: actions/checkout@v4
         with:
@@ -513,14 +520,28 @@ jobs:
         uses: actions/setup-python@v5
         with:
           python-version: '3.11'
-      - name: Generate Codebase Markdown
+      - name: Generate Smart Docs & Dashboard
         run: python scripts/generate_smart_docs.py
-      - name: Commit and Push Docs
+      - name: Commit and Push Docs to Repo
+        id: push_docs
         run: |
           git config --global user.name "github-actions[bot]"
-          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
-          git add docs/
-          git diff-index --quiet HEAD || (git commit -m "docs: auto-generate smart codebase docs & changelog [skip ci]" && git push) || echo "No changes to commit"
+          git config --global user.email "github-actions[bot]@users.noreply.github.com"
+          git add docs/autogen/
+          git diff-index --quiet HEAD || (git commit -m "docs: auto-update codebase docs & dashboard [skip ci]" && git push) || echo "No changes to commit"
+      - name: Setup GitHub Pages Environment
+        if: github.ref == 'refs/heads/main'
+        uses: actions/configure-pages@v5
+      - name: Upload Artifact to Pages
+        if: github.ref == 'refs/heads/main'
+        uses: actions/upload-pages-artifact@v3
+        with:
+          path: 'docs/autogen'
+      - name: Deploy to GitHub Pages
+        id: deployment
+        if: github.ref == 'refs/heads/main'
+        uses: actions/deploy-pages@v4
+
 
   ci-report:
     name: 📊 Generate Smart CI Report

```
