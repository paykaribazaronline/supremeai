# 📋 Commit 30b2c03ebe9dc12a14768667dffa68e20064ba24

## Commit Stats
```
commit 30b2c03ebe9dc12a14768667dffa68e20064ba24
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Sun Jul 5 05:07:16 2026 +0600

    test: fix e2e test timeouts by waiting for UI to render

 tests/e2e/accessibility.spec.ts | 2 ++
 tests/e2e/chat.spec.ts          | 3 ++-
 2 files changed, 4 insertions(+), 1 deletion(-)

```

## Diff Detail
```diff
commit 30b2c03ebe9dc12a14768667dffa68e20064ba24
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Sun Jul 5 05:07:16 2026 +0600

    test: fix e2e test timeouts by waiting for UI to render

diff --git a/tests/e2e/accessibility.spec.ts b/tests/e2e/accessibility.spec.ts
index 88bbc34af..5d398bfa4 100644
--- a/tests/e2e/accessibility.spec.ts
+++ b/tests/e2e/accessibility.spec.ts
@@ -4,6 +4,7 @@ import AxeBuilder from '@axe-core/playwright';
 test.describe('Accessibility Tests (WCAG)', () => {
     test('Homepage should not have any automatically detectable accessibility issues', async ({ page }) => {
         await page.goto('/');
+        await page.waitForSelector('[data-testid="dashboard-sidebar"]', { state: 'visible', timeout: 15000 });
 
         const accessibilityScanResults = await new AxeBuilder({ page }).analyze();
 
@@ -17,6 +18,7 @@ test.describe('Accessibility Tests (WCAG)', () => {
 
     test('Admin Dashboard should be accessible', async ({ page }) => {
         await page.goto('/admin'); // আপনার অ্যাডমিন পেজের URL
+        await page.waitForSelector('text=SupremeAI', { state: 'visible', timeout: 15000 });
 
         const accessibilityScanResults = await new AxeBuilder({ page }).analyze();
 
diff --git a/tests/e2e/chat.spec.ts b/tests/e2e/chat.spec.ts
index 430b7ede7..2af9dc93e 100644
--- a/tests/e2e/chat.spec.ts
+++ b/tests/e2e/chat.spec.ts
@@ -1,7 +1,8 @@
 import { test, expect } from '@playwright/test';
 
 test('Chat sends message', async ({ page }) => {
-  await page.goto('/');
+  await page.goto('/#/workspace');
+  await page.waitForSelector('[data-testid="chat-input"]', { state: 'visible', timeout: 15000 });
   await page.fill('[data-testid="chat-input"]', 'Hello SupremeAI!');
   await page.click('[data-testid="chat-submit"]');
   await expect(page.getByText('Hello SupremeAI!').first()).toBeVisible();

```
