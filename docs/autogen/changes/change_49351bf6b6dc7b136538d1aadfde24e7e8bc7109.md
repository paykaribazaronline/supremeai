# 📋 Commit 49351bf6b6dc7b136538d1aadfde24e7e8bc7109

## Commit Stats
```
commit 49351bf6b6dc7b136538d1aadfde24e7e8bc7109
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 00:57:22 2026 +0600

    fix: CI/CD artifact path for dist-admin and add Flutter real-time SSE stream sync

 .github/workflows/supreme-core-ci.yml              |  2 +-
 .../lib/providers/orchestration_provider.dart      | 29 ++++++++++++++++++++++
 2 files changed, 30 insertions(+), 1 deletion(-)

```

## Diff Detail
```diff
commit 49351bf6b6dc7b136538d1aadfde24e7e8bc7109
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 00:57:22 2026 +0600

    fix: CI/CD artifact path for dist-admin and add Flutter real-time SSE stream sync

diff --git a/.github/workflows/supreme-core-ci.yml b/.github/workflows/supreme-core-ci.yml
index 20c186a9a1..8171e813de 100644
--- a/.github/workflows/supreme-core-ci.yml
+++ b/.github/workflows/supreme-core-ci.yml
@@ -459,7 +459,7 @@ jobs:
         with:
           name: frontend-dist
           path: |
-            apps/studio-client/dist
+            apps/studio-client/dist-admin
             apps/web-chat/dist
           retention-days: 1
 
diff --git a/apps/mobile/lib/providers/orchestration_provider.dart b/apps/mobile/lib/providers/orchestration_provider.dart
index 7be27b7f08..dc9ebdad53 100644
--- a/apps/mobile/lib/providers/orchestration_provider.dart
+++ b/apps/mobile/lib/providers/orchestration_provider.dart
@@ -83,6 +83,35 @@ class OrchestrationProvider with ChangeNotifier {
   bool _isOnline = true;
   List<Map<String, dynamic>> _offlineQueue = [];
 
+  Map<String, dynamic> activeAgentMetrics = {};
+
+  void initRealTimeTaskStream(String taskId, String authToken) {
+    final url = Uri.parse('https://supremeai-api-lhlwyikwlq-uc.a.run.app/api/task/stream/$taskId');
+    
+    final request = http.Request('GET', url)
+      ..headers['Authorization'] = 'Bearer $authToken'
+      ..headers['Accept'] = 'text/event-stream';
+
+    request.send().then((response) {
+      response.stream.transform(utf8.decoder).listen((data) {
+        if (data.startsWith('data:')) {
+          try {
+            final jsonString = data.replaceFirst('data:', '').trim();
+            if (jsonString.isNotEmpty) {
+              final metricUpdate = jsonDecode(jsonString);
+              activeAgentMetrics = metricUpdate;
+              notifyListeners();
+            }
+          } catch (e) {
+            debugPrint('🔴 Flutter SSE Parse Error: $e');
+          }
+        }
+      });
+    }).catchError((error) {
+      debugPrint('🔴 Flutter SSE Stream Error: $error');
+    });
+  }
+
   bool get isLoading => _isLoading;
   Map<String, dynamic>? get lastResult => _lastResult;
   OrchestrationError? get error => _error;

```
