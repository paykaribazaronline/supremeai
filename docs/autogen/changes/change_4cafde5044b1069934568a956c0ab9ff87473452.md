# 📋 Commit 4cafde5044b1069934568a956c0ab9ff87473452

## Commit Stats
```
commit 4cafde5044b1069934568a956c0ab9ff87473452
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 18:46:32 2026 +0600

    fix: initialize litellm proxy settings to fix swarm orchestrator test
    
    - LLMGateway এ litellm.use_litellm_proxy = False যোগ করা হয়েছে
    - test_swarm_orchestrator.py এ litellm fixture যোগ করা হয়েছে
    - conftest.py এ global litellm configuration fixture যোগ করা হয়েছে
    
    ফিক্স: BadRequestError - 'types.SimpleNamespace' object has no attribute 'use_litellm_proxy'
    
    এটা test environment এ litellm settings সঠিকভাবে initialize না হওয়ার সমস্যা ছিল।

 backend/core/llm_gateway.py              |  2 ++
 backend/tests/conftest.py                | 15 +++++++++++++++
 backend/tests/test_swarm_orchestrator.py | 12 ++++++++++++
 3 files changed, 29 insertions(+)

```

## Diff Detail
```diff
commit 4cafde5044b1069934568a956c0ab9ff87473452
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 18:46:32 2026 +0600

    fix: initialize litellm proxy settings to fix swarm orchestrator test
    
    - LLMGateway এ litellm.use_litellm_proxy = False যোগ করা হয়েছে
    - test_swarm_orchestrator.py এ litellm fixture যোগ করা হয়েছে
    - conftest.py এ global litellm configuration fixture যোগ করা হয়েছে
    
    ফিক্স: BadRequestError - 'types.SimpleNamespace' object has no attribute 'use_litellm_proxy'
    
    এটা test environment এ litellm settings সঠিকভাবে initialize না হওয়ার সমস্যা ছিল।

diff --git a/backend/core/llm_gateway.py b/backend/core/llm_gateway.py
index 7494a319d..f0cb0c0a3 100644
--- a/backend/core/llm_gateway.py
+++ b/backend/core/llm_gateway.py
@@ -24,6 +24,8 @@ class LLMGateway:
         # Configure litellm global settings
         litellm.drop_params = True
         litellm.telemetry = False
+        # বাংলা মন্তব্য: লিটেলএলএম প্রক্সি সেটিংস নিশ্চিত করা (test compatibility)
+        litellm.use_litellm_proxy = False
 
         # Initialize semantic cache engine
         from core.semantic_cache import SemanticCache
diff --git a/backend/tests/conftest.py b/backend/tests/conftest.py
index 691a397ae..6ae33cf03 100644
--- a/backend/tests/conftest.py
+++ b/backend/tests/conftest.py
@@ -115,3 +115,18 @@ def bypass_jwt_auth():
     for p in patches:
         with contextlib.suppress(Exception):
             p.stop()
+
+
+@pytest.fixture(autouse=True)
+def configure_litellm():
+    """টেস্টের জন্য litellm সেটিংস কনফিগার করুন"""
+    # বাংলা মন্তব্য: লিটেলএলএম প্রক্সি এবং টেলিমেট্রি সেটিংস নিশ্চিত করা
+    try:
+        import litellm
+        litellm.use_litellm_proxy = False
+        litellm.drop_params = True
+        litellm.telemetry = False
+    except Exception:
+        pass
+    yield
+
diff --git a/backend/tests/test_swarm_orchestrator.py b/backend/tests/test_swarm_orchestrator.py
index 3185273e4..e724d5465 100644
--- a/backend/tests/test_swarm_orchestrator.py
+++ b/backend/tests/test_swarm_orchestrator.py
@@ -1,7 +1,19 @@
 import pytest
 from unittest.mock import patch, AsyncMock
+import litellm
 from core.swarm_orchestrator import SwarmOrchestrator
 
+
+@pytest.fixture(autouse=True)
+def setup_litellm():
+    """কনফিগার করুন litellm সেটিংস প্রতিটি টেস্টের আগে"""
+    # বাংলা মন্তব্য: লিটেলএলএম প্রক্সি সেটিংস নিশ্চিত করা
+    litellm.use_litellm_proxy = False
+    litellm.drop_params = True
+    litellm.telemetry = False
+    yield
+
+
 @pytest.mark.anyio
 async def test_swarm_orchestrator_runs_entire_graph():
     orchestrator = SwarmOrchestrator()

```
