# 📋 Commit 7ae15cae946b33f1fc7866fa7ef9b7690306842e

## Commit Stats
```
commit 7ae15cae946b33f1fc7866fa7ef9b7690306842e
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Tue Jul 7 13:06:14 2026 +0600

    Fix request storm and infinite retry loop

 apps/studio-client/src/App.tsx                 | 22 +++++++++++++++++++++-
 apps/studio-client/src/utils/apiInterceptor.ts | 26 ++++++++++++++++++++++++++
 2 files changed, 47 insertions(+), 1 deletion(-)

```

## Diff Detail
```diff
commit 7ae15cae946b33f1fc7866fa7ef9b7690306842e
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Tue Jul 7 13:06:14 2026 +0600

    Fix request storm and infinite retry loop

diff --git a/apps/studio-client/src/App.tsx b/apps/studio-client/src/App.tsx
index 49c4502b2..b8c056717 100644
--- a/apps/studio-client/src/App.tsx
+++ b/apps/studio-client/src/App.tsx
@@ -3,7 +3,18 @@ import { Routes, Route } from "react-router-dom";
 import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
 import { useStore } from "./store/useStore";
 
-const queryClient = new QueryClient();
+const queryClient = new QueryClient({
+  defaultOptions: {
+    queries: {
+      retry: (failureCount, error: any) => {
+        if (error?.status === 401 || error?.status === 403) return false;
+        return failureCount < 3;
+      },
+      retryDelay: 5000,
+      refetchOnWindowFocus: false,
+    },
+  },
+});
 import { useAdminStore } from "./store/adminStore";
 import { AdminConsole } from "./components/admin/AdminConsole";
 import { UserDashboard } from "./components/customer/UserDashboard";
@@ -313,6 +324,15 @@ export const App: React.FC = () => {
     eventSource.onerror = () => {
       console.error("🔴 [SYSTEM CRITICAL] SSE Stream severed. SupremeAI Server is OFFLINE.");
       setServerStatus(false);
+      eventSource.close(); // Prevent infinite native retries
+    };
+    
+    eventSource.onmessage = (e) => {
+      if (e.data && (e.data.includes('auth_error') || e.data.includes('401'))) {
+         console.error("🔴 SSE Auth Error: Closing stream to prevent storm.");
+         eventSource.close();
+         setServerStatus(false);
+      }
     };
 
     return () => {
diff --git a/apps/studio-client/src/utils/apiInterceptor.ts b/apps/studio-client/src/utils/apiInterceptor.ts
index a8dd9bfca..e59975f3e 100644
--- a/apps/studio-client/src/utils/apiInterceptor.ts
+++ b/apps/studio-client/src/utils/apiInterceptor.ts
@@ -1,9 +1,35 @@
+import { getAdminToken } from '../services/adminTokenStore';
+import { getApiBaseUrl } from './api';
+
 export function setupGlobalFetchInterceptor() {
   if (typeof window === 'undefined') return;
 
   const originalFetch = window.fetch;
 
   window.fetch = async function (...args) {
+    let [url, options] = args;
+    const apiBase = getApiBaseUrl();
+
+    if (typeof url === 'string' && url.startsWith(apiBase)) {
+      const token = getAdminToken();
+      
+      if (url.includes('/admin-api/')) {
+        if (!token) {
+           console.warn("Blocked unauthorized API request to prevent storm:", url);
+           return Promise.reject(new Error("No token found"));
+        }
+      }
+
+      if (token) {
+        options = options || {};
+        options.headers = {
+          ...options.headers,
+          'Authorization': `Bearer ${token}`
+        };
+        args[1] = options;
+      }
+    }
+
     try {
       const response = await originalFetch.apply(this, args);
       

```
