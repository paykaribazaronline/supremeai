# 📋 Commit e2c0631573c07be3d1748e30f40f885de91f038d

## Commit Stats
```
commit e2c0631573c07be3d1748e30f40f885de91f038d
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 16:42:35 2026 +0600

    fix(ci): fix poetry path issue by using pipx for installation

 .github/workflows/nightly-maintenance.yml | 33 +++++++++++++--------
 .github/workflows/supreme-core-ci.yml     | 49 +++++++++++++++++++------------
 2 files changed, 52 insertions(+), 30 deletions(-)

```

## Diff Detail
```diff
commit e2c0631573c07be3d1748e30f40f885de91f038d
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Wed Jul 8 16:42:35 2026 +0600

    fix(ci): fix poetry path issue by using pipx for installation

diff --git a/.github/workflows/nightly-maintenance.yml b/.github/workflows/nightly-maintenance.yml
index e0016c015..2a8d73cab 100644
--- a/.github/workflows/nightly-maintenance.yml
+++ b/.github/workflows/nightly-maintenance.yml
@@ -70,6 +70,11 @@ jobs:
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
+      - name: Install Poetry
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          cd backend && poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
@@ -78,10 +83,8 @@ jobs:
           key: venv-${{ runner.os }}-${{ hashFiles('backend/poetry.lock') }}
       - name: Install Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
-        run: |
-          pip install poetry
-          cd backend && poetry config virtualenvs.in-project true
-          cd backend && poetry install --with dev --without ml
+        working-directory: backend
+        run: poetry install --with dev --without ml
       - name: Auto-Generate Missing Tests
         env:
           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
@@ -304,6 +307,11 @@ jobs:
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
+      - name: Install Poetry
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          cd backend && poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
@@ -312,10 +320,8 @@ jobs:
           key: venv-${{ runner.os }}-${{ hashFiles('backend/poetry.lock') }}
       - name: Install Python Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
-        run: |
-          pip install poetry
-          cd backend && poetry config virtualenvs.in-project true
-          cd backend && poetry install --with dev --without ml
+        working-directory: backend
+        run: poetry install --with dev --without ml
       - name: Install Dependencies
         run: pnpm install --frozen-lockfile
       - name: Install Playwright Browsers
@@ -358,6 +364,12 @@ jobs:
         uses: actions/setup-python@v5
         with:
           python-version: '3.11'
+      - name: Install Poetry
+        working-directory: backend
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
@@ -367,10 +379,7 @@ jobs:
       - name: Install Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
         working-directory: backend
-        run: |
-          pip install poetry
-          poetry config virtualenvs.in-project true
-          poetry install --sync --without ml
+        run: poetry install --sync --without ml
       - name: Start Backend for Testing
         working-directory: backend
         env:
diff --git a/.github/workflows/supreme-core-ci.yml b/.github/workflows/supreme-core-ci.yml
index 5a45f5d4c..8665cf695 100644
--- a/.github/workflows/supreme-core-ci.yml
+++ b/.github/workflows/supreme-core-ci.yml
@@ -117,6 +117,11 @@ jobs:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
 
+      - name: Install Poetry
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
@@ -126,10 +131,7 @@ jobs:
       - name: Install Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
         working-directory: backend
-        run: |
-          pip install poetry
-          poetry config virtualenvs.in-project true
-          poetry install --sync --with dev --without ml,tools
+        run: poetry install --sync --with dev --without ml,tools
 
       - name: 🛡️ Safety Guard - File Protection Validation
         id: safety_guard
@@ -202,6 +204,11 @@ jobs:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
       
+      - name: Install Poetry
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
@@ -211,10 +218,7 @@ jobs:
       - name: Install Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
         working-directory: backend
-        run: |
-          pip install poetry
-          poetry config virtualenvs.in-project true
-          poetry install --sync --with dev --without ml,tools
+        run: poetry install --sync --with dev --without ml,tools
       
       - name: 🧹 Lint Code (Auto-Fix & Warning Mode)
         working-directory: backend
@@ -496,21 +500,27 @@ jobs:
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
+      - name: Install Poetry
+        working-directory: backend
+        run: |
+          pipx install poetry
+          poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
         with:
           path: backend/.venv
           key: venv-${{ runner.os }}-${{ hashFiles('backend/poetry.lock') }}
-      - name: Install Backend Dependencies & Start Server
+      - name: Install Backend Dependencies (Only on Cache Miss)
+        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
+        working-directory: backend
+        run: poetry install --sync --with dev --without ml,tools
+      - name: Start Backend Server
         working-directory: backend
         env:
           ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
           SUPREMEAI_API_URL: http://127.0.0.1:8000
-        run: |
-          pip install poetry
-          poetry install --sync --with dev --without ml,tools
-          poetry run uvicorn main:app --port 8000 &
+        run: poetry run uvicorn main:app --port 8000 &
       - name: Install Playwright Browsers
         run: pnpm exec playwright install --with-deps
       - name: Start Frontend Preview Server
@@ -761,18 +771,21 @@ jobs:
         uses: actions/setup-python@v5
         with:
           python-version: '3.11'
+      - name: Install Poetry
+        run: |
+          pipx install poetry
+          pip install litellm pyyaml httpx
+          cd backend && poetry config virtualenvs.in-project true
       - name: Load Cached Virtualenv
         id: cached-poetry-dependencies
         uses: actions/cache@v4
         with:
           path: backend/.venv
           key: venv-${{ runner.os }}-${{ hashFiles('backend/poetry.lock') }}
-      - name: Install dependencies (Only on Cache Miss)
+      - name: Install Dependencies (Only on Cache Miss)
         if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
-        run: |
-          pip install poetry litellm pyyaml httpx
-          cd backend && poetry config virtualenvs.in-project true
-          cd backend && poetry install --sync --with dev --without ml,tools
+        working-directory: backend
+        run: poetry install --sync --with dev --without ml,tools
           
       - name: 🤖 Generate AI Architecture Docs (ADR, DFD, OpenAPI)
         env:

```
