# 📋 Commit 3664f0653ab01023decf4676699b67bf289f4921

## Commit Stats
```
commit 3664f0653ab01023decf4676699b67bf289f4921
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Tue Jul 7 19:33:48 2026 +0600

    fix(lint): SIM102 replace nested if with single if in config.py

 backend/core/config.py | 5 ++---
 1 file changed, 2 insertions(+), 3 deletions(-)

```

## Diff Detail
```diff
commit 3664f0653ab01023decf4676699b67bf289f4921
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Tue Jul 7 19:33:48 2026 +0600

    fix(lint): SIM102 replace nested if with single if in config.py

diff --git a/backend/core/config.py b/backend/core/config.py
index 27a594599..232aba1f8 100644
--- a/backend/core/config.py
+++ b/backend/core/config.py
@@ -204,9 +204,8 @@ class Settings(BaseSettings):
                 raise RuntimeError(
                     f"Missing required configurations for production: {', '.join(missing)}"
                 )
-        if self.env.lower() in {"production", "staging"}:
-            if not self.ci_webhook_secret:
-                raise RuntimeError("Missing required configuration for staging/production: secure CI_WEBHOOK_SECRET")
+        if self.env.lower() in {"production", "staging"} and not self.ci_webhook_secret:
+            raise RuntimeError("Missing required configuration for staging/production: secure CI_WEBHOOK_SECRET")
 
 
 settings = Settings()

```
