# 📋 Commit 4b2946d03658d1d2d83fefbb7199700ffdf74fa6

## Commit Stats
```
commit 4b2946d03658d1d2d83fefbb7199700ffdf74fa6
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 22:04:03 2026 +0600

    fix: update github actions cache cleanup to gracefully handle HTTP 403 errors

 .github/workflows/nightly-maintenance.yml | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

```

## Diff Detail
```diff
commit 4b2946d03658d1d2d83fefbb7199700ffdf74fa6
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Fri Jul 3 22:04:03 2026 +0600

    fix: update github actions cache cleanup to gracefully handle HTTP 403 errors

diff --git a/.github/workflows/nightly-maintenance.yml b/.github/workflows/nightly-maintenance.yml
index af498f36d..77b099d4e 100644
--- a/.github/workflows/nightly-maintenance.yml
+++ b/.github/workflows/nightly-maintenance.yml
@@ -96,11 +96,15 @@ jobs:
           REDIS_URL: ${{ secrets.REDIS_URL }}
         run: |
           python tools/cache_cleanup.py
-      - name: Prune stale GitHub caches
+      - name: Clean Caches
         env:
           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         run: |
-          gh cache list --repo ${{ github.repository }} --limit 100 | awk '{print $1}' | xargs -r -I {} gh cache delete {} --repo ${{ github.repository }}
+          # শুধুমাত্র নির্দিষ্ট কিছু ক্যাশ ডিলিট করার চেষ্টা করুন
+          gh cache list --limit 100 --json key -q '.[].key' | while read key; do
+            echo "Deleting cache: $key"
+            gh cache delete "$key" --confirm || echo "Could not delete $key - Skipping"
+          done
 
   # ----------------------------------------------------
   # 📦 DEPENDENCY UPDATE (Dependabot বিকল্প)

```
