# 📋 Commit 0a226338226b753f27112cedee3289e3e48aec7b

## Commit Stats
```
commit 0a226338226b753f27112cedee3289e3e48aec7b
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 03:03:27 2026 +0600

    fix: remove unused imports and add noqa for broad exception handling

 backend/core/auth_middleware.py | 3 ---
 backend/core/event_bus.py       | 2 +-
 backend/core/lifespan.py        | 2 +-
 3 files changed, 2 insertions(+), 5 deletions(-)

```

## Diff Detail
```diff
commit 0a226338226b753f27112cedee3289e3e48aec7b
Author: SupremeAI CI Bot <ci-bot@supremeai.dev>
Date:   Thu Jul 9 03:03:27 2026 +0600

    fix: remove unused imports and add noqa for broad exception handling

diff --git a/backend/core/auth_middleware.py b/backend/core/auth_middleware.py
index e004905bfa..efc8b27a04 100644
--- a/backend/core/auth_middleware.py
+++ b/backend/core/auth_middleware.py
@@ -38,8 +38,6 @@ class AuthMiddleware:
         path = scope.get("path", "")
         # বাংলা মন্তব্য: ASGI request scope variants-এর জন্য path resolution fallback যোগ করা হলো।
         if not path and scope.get("raw_path"):
-            import contextlib
-
             # বাংলা মন্তব্য: P1 Fix — Exception Swallowing পরিহার করে নির্দিষ্ট এক্সেপশন ক্যাচ করা হলো
             try:
                 path = scope["raw_path"].decode("utf-8").split("?")[0]
@@ -164,7 +162,6 @@ class AuthMiddleware:
 
         if is_test:
             expected_token = os.getenv("SUPREMEAI_API_TOKEN")
-            import secrets
             # বাংলা মন্তব্য: P1 Fix — test bypass logic সংশোধন করা হলো।
             # আগে: expected_token সেট থাকলে এবং match না করলে `pass` (fallthrough) হতো —
             # তারপর নিচের `enabled` check-এ পড়ে production logic execute হতো।
diff --git a/backend/core/event_bus.py b/backend/core/event_bus.py
index 49d02e428d..682bbcbcb3 100644
--- a/backend/core/event_bus.py
+++ b/backend/core/event_bus.py
@@ -71,7 +71,7 @@ class ErrorEventBus:
                         context={"original_module": event.module, "original_error": str(listener_exc)[:200]},
                     )
                     asyncio.create_task(self.emit_async(backup_event))
-                except Exception:
+                except Exception:  # noqa: BLE001
                     pass  # Ultimate fallback — no infinite recursion
 
 
diff --git a/backend/core/lifespan.py b/backend/core/lifespan.py
index 3f83b45e4e..25064c7071 100644
--- a/backend/core/lifespan.py
+++ b/backend/core/lifespan.py
@@ -108,7 +108,7 @@ async def app_lifespan(app):
             await init_db_pool(db_url)
             logger.info("⚡ PgBouncer connection pool successfully initialized at startup.")
             await _ensure_api_key_tables()
-    except Exception as exc:
+    except Exception as exc:  # noqa: BLE001
         # বাংলা মন্তব্য: P1 Fix — DB fail হলে startup crash করা হবে না।
         # DB-dependent features gracefully disabled হবে।
         # Health endpoint, SSE stream, config cache সব চলবে DB ছাড়া।

```
