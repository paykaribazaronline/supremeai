# ============================================================================
# config >> audit-rules.yml
# project >> SupremeAI 2.0
# purpose >> Audit logging
# module >> config
# ============================================================================
      message: "Local redis configured for staging or production"
    - pattern: "LOG_LEVEL=debug"
      message: "Verbose logging in production"
  MEDIUM:
    - pattern: "MISSING:.*"
      message: "Required variable is missing"

required_envs:
  development:
    - DATABASE_URL
    - REDIS_URL
    - API_KEY
  staging:
    - DATABASE_URL
    - REDIS_URL
    - API_KEY
    - SENTRY_DSN
  production:
    - DATABASE_URL
    - REDIS_URL
    - API_KEY
    - SENTRY_DSN
    - SSL_CERT
