# ============================================================================
# config >> docker-limits.yml
# project >> SupremeAI 2.0
# purpose >> Docker settings
# module >> config
# ============================================================================
# ============================================================================
# config >> docker-limits.yml
# project >> SupremeAI 2.0
# purpose >> Docker settings
# module >> config
# ============================================================================
# ============================================================================\n# config >> docker-limits.yml\n# project >> SupremeAI 2.0\n# purpose >> Docker container settings\n# module >> config\n# ============================================================================\n# ============================================================================
# config >> docker-limits.yml
# project >> SupremeAI 2.0
# purpose >> Docker container settings
# module >> config
# ============================================================================
# ============================================================================
# Config >> docker-limits.yml
# Project >> SupremeAI 2.0
# Purpose >> Docker container settings
# Module >> config
# ============================================================================
# ============================================================================
# Config >> docker-limits.yml
# Project >> SupremeAI 2.0
# Purpose >> Docker container settings
# Module >> config
# ============================================================================
# ============================================================================
# Config: docker-limits.yml
# Project: SupremeAI 2.0
# Purpose: Docker container settings
# Module: config
# ============================================================================
# ============================================================================
# Config: docker-limits.yml
# Project: SupremeAI 2.0
# Purpose: Docker container settings
# Module: config
# ============================================================================
limits:
  backend:
    max_size_mb: 500
    warn_size_mb: 300
  studio-client:
    max_size_mb: 150
    warn_size_mb: 100
  default:
    max_size_mb: 500
    warn_size_mb: 300
