import 'package:flutter/material.dart';

import 'unified_admin_screen.dart';

Widget buildAdminSurface({
  required String dashboardUrl,
  required bool isTestMode,
}) {
  // Return unified admin screen for consistent experience across platforms
  return const UnifiedAdminScreen();
}
