name: SupremeAI_Master_Pipeline

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  changes:
    name: Detect_Changes
    runs-on: ubuntu-latest
    outputs:
      backend: "${{ steps.filter.outputs.backend }}"
      frontend: "${{ steps.filter.outputs.frontend }}"
      plugin: "${{ steps.filter.outputs.plugin }}"
      vscode_extension: "${{ steps.filter.outputs.vscode_extension }}"
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'src/**'
              - 'Dockerfile'
              - 'build.gradle*'
              - '.github/workflows/**'
            frontend:
              - 'supremeai/**'
              - 'firebase.json'
              - '.github/workflows/**'
            plugin:
              - 'supremeai-intellij-plugin/**'
              - '.github/workflows/**'
            vscode_extension:
              - 'supremeai-vscode-extension/**'
              - '.github/workflows/**'

  codeql:
    name: CodeQL_Analysis
    needs: [changes]
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: CodeQL_Init
        uses: github/codeql-action/init@v3
        with:
          languages: "java"
          build-mode: "none"
      - name: Run_CodeQL
        uses: github/codeql-action/analyze@v3

  secret-scan:
    name: Secret_Scanning
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Secret_Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: "./"
          extra_args: "--only-verified"

  plugin-build:
    name: Plugin_Build
    needs: [changes, secret-scan]
    if: needs.changes.outputs.plugin == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup_Java
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"
      - name: Build_Plugin
        run: "./gradlew :supremeai-intellij-plugin:buildPlugin"
      - name: Store_Plugin_Path
        run: echo "PLUGIN_PATH=supremeai-intellij-plugin/build/distributions/*.zip" >> $GITHUB_ENV

  vscode-extension-build:
    name: VSCode_Extension_Build
    needs: [changes, secret-scan]
    if: needs.changes.outputs.vscode_extension == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup_Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "supremeai-vscode-extension/package-lock.json"
      - name: Install_Dependencies
        run: |
          cd supremeai-vscode-extension
          npm install
      - name: Lint_and_Compile
        run: |
          cd supremeai-vscode-extension
          npm run lint
          npm run compile
      - name: Package_Extension
        run: |
          cd supremeai-vscode-extension
          npx vsce package --no-git-tag-version
      - name: Store_VSIX_Path
        run: echo "VSIX_PATH=supremeai-vscode-extension/*.vsix" >> $GITHUB_ENV

  java-build-and-test:
    name: Java_Build_And_Test
    needs: [changes, secret-scan]
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup_Java
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "gradle"
      - name: Build_And_Test
        run: "./gradlew build -x jacocoTestCoverageVerification"
      - name: Upload_JAR
        uses: actions/upload-artifact@v4
        with:
          name: java-backend-jar
          path: "build/libs/*.jar"

  deploy-backend:
    name: Deploy_Backend
    needs: [java-build-and-test, codeql]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate_GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_SUPREMEAI_A }}"
      - name: Setup_GCloud
        uses: google-github-actions/setup-gcloud@v2
      - name: Deploy_Backend_To_Cloud_Run
        run: |
          gcloud run deploy supremeai \
            --source . \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --memory 2Gi \
            --cpu 2 \
            --timeout 300 \
            --set-env-vars=SPRING_PROFILES_ACTIVE=cloud,FIREBASE_PROJECT_ID=supremeai-a,"JAVA_OPTS=-Xms256m -Xmx1024m -XX:+UseG1GC"
      - name: Get_Backend_URL
        run: echo "BACKEND_URL=$(gcloud run services describe supremeai --platform managed --region us-central1 --format='value(status.url)')" >> $GITHUB_ENV
      - name: Show_Cloud_Build_Logs
        if: failure()
        run: |
          echo "Fetching latest Cloud Build logs..."
          BUILD_ID=$(gcloud builds list --project=supremeai-a --limit=1 --format='value(id)')
          gcloud builds log $BUILD_ID --project=supremeai-a

  flutter-build-and-test:
    name: Flutter_Build_And_Test
    needs: [changes, secret-scan]
    if: needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup_Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.27.0"
          cache: true
      - name: Test_And_Build
        working-directory: ./supremeai
        run: |
          flutter pub get
          flutter test
          flutter build web --release
          flutter build apk --release --target-platform android-arm64
      - name: Upload_Web
        uses: actions/upload-artifact@v4
        with:
          name: customer-web-build
          path: supremeai/build/web
      - name: Store_Artifact_Paths
        run: |
          echo "FRONTEND_URL=https://supremeai-a.web.app/" >> $GITHUB_ENV
          echo "APK_PATH=supremeai/build/app/outputs/flutter-apk/app-release.apk" >> $GITHUB_ENV

  deploy-frontend:
    name: Deploy_Frontend
    needs: [flutter-build-and-test, codeql]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Download_Web
        uses: actions/download-artifact@v4
        with:
          name: customer-web-build
          path: "supremeai"
      - name: Firebase_Deploy
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_SUPREMEAI_A }}"
          projectId: "supremeai-a"
          channelId: "live"
          entryPoint: "./supremeai"

  workflow-summary:
    name: Workflow_Summary
    needs: [deploy-backend, deploy-frontend, plugin-build, vscode-extension-build]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Generate_Summary
        run: |
          echo "### Workflow Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Backend URL:** ${{ env.BACKEND_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "**Frontend URL:** ${{ env.FRONTEND_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "**APK Path:** `${{ env.APK_PATH }}`" >> $GITHUB_STEP_SUMMARY
          echo "**Plugin Path:** `${{ env.PLUGIN_PATH }}`" >> $GITHUB_STEP_SUMMARY
          echo "**VS Code Extension Path:** `${{ env.VSIX_PATH }}`" >> $GITHUB_STEP_SUMMARY

  health-check:
    name: Health_Check
    needs: [deploy-backend, deploy-frontend]
    if: always() && (needs.deploy-backend.result == 'success' || needs.deploy-frontend.result == 'success')
    runs-on: ubuntu-latest
    steps:
      - name: Check_Status
        run: "curl -sf https://supremeai-a.web.app/ || echo 'Site check skipped'"

  notify:
    name: Notification
    needs: [health-check]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Status_Update
        run: "echo 'Deployment process completed with status: ${{ needs.health-check.result }}'"
