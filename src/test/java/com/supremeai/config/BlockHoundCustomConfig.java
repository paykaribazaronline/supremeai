package com.supremeai.config;

/**
 * Automatically loaded by BlockHound via Java SPI.
 * Fails the CI/CD pipeline tests if any reactive thread calls a blocking API.
 * [OFFLINE MODE: BlockHound integration commented out due to lack of offline library jar]
 */
public class BlockHoundCustomConfig {
    // BlockHound is disabled in offline test runs
}