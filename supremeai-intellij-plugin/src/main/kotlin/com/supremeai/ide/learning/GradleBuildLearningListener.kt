package com.supremeai.ide.learning

/**
 * Placeholder listener for Gradle build learning hooks.
 *
 * The Android Studio target used by this plugin does not expose the execution
 * APIs that the previous implementation depended on, so the real build-failure
 * capture is handled by [GradleFailureDetector] via external system callbacks.
 */
class GradleBuildLearningListener
