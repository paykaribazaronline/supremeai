{
  "entries": [
    {
      "task": "localhost login firebase emulator failed to fetch",
      "solution": "[LOCAL-SEED] Use Firebase client signInWithEmailAndPassword only. Make backend /api/auth/firebase-login exchange optional with try/catch fallback to ID token. Update VITE_API_URL to Functions emulator port (5003).",
      "confidence": 0.95,
      "tags": ["login", "emulator", "cors", "firebase"]
    },
    {
      "task": "ai providers tab empty no gcloud models",
      "solution": "[SMART] Auto-seed Firebase + Vertex AI providers from env + known gcloud config. Return from /getConfiguredProviders function instead of requiring manual database entries.",
      "confidence": 0.9,
      "tags": ["providers", "gcloud", "vertex", "smart-discovery"]
    },
    {
      "task": "functions emulator cors preflight blocked",
      "solution": "[FIX] Add global allowCors wrapper in functions/index.js for all onRequest exports. Set Access-Control-Allow-Origin * and handle OPTIONS preflight.",
      "confidence": 1.0,
      "tags": ["cors", "emulator", "functions"]
    },
    {
      "task": "admin dashboard multiple api calls failing in emulator",
      "solution": "[RESILIENCE] Wrap fetchWithAuth in try/catch. Return 503 Response on network/CORS error instead of throwing. Prevents console spam and keeps UI stable.",
      "confidence": 0.95,
      "tags": ["resilience", "emulator", "fetch"]
    },
    {
      "task": "hello hi greetings how to use",
      "solution": "[LOCAL-SEED] SupremeAI is an AI-powered software engineering assistant. You can ask me to write, debug, or refactor code, run build/test commands, and manage deployments — all directly from the CLI.",
      "confidence": 1.0,
      "tags": ["greetings", "chat", "offline"]
    },
    {
      "task": "hello who are you what is supremeai",
      "solution": "[LOCAL-SEED] SupremeAI is a multi-provider AI engineering platform. It connects to multiple AI providers, manages knowledge with a self-learning loop, self-heals from errors, and runs a secure admin dashboard at http://localhost:3000/admin.",
      "confidence": 1.0,
      "tags": ["greetings", "identity", "offline", "docs"]
    },
    {
      "task": "status health check system running",
      "solution": "[LOCAL-SEED] 1. Run: curl http://localhost:8080/actuator/health 2. Check: http://localhost:3000/admin/dashboard 3. Green = all services healthy. Yellow = degraded. Red = down. Check backend.log for details.",
      "confidence": 0.97,
      "tags": ["health", "status", "offline", "monitoring"]
    },
    {
      "task": "build compile gradle maven project failed errors",
      "solution": "[LOCAL-SEED] 1. Run: ./gradlew clean compileJava 2. If still failing, search backend.log or build_errors.log for last stack trace 3. Fix the top file in the stack trace first — build errors are almost always a single missing import or deprecation 4. Re-run build",
      "confidence": 0.95,
      "tags": ["build", "compile", "gradle", "offline"]
    },
    {
      "task": "database connection firestore timeout error connection refused",
      "solution": "[LOCAL-SEED] 1. Check cloud functions emulator: firebase emulators:start 2. Verify GOOGLE_APPLICATION_CREDENTIALS points to valid service-account.json 3. If using service account, ensure the file is in the project root 4. Restart the application — Firestore clients need fresh context",
      "confidence": 0.93,
      "tags": ["database", "firestore", "connection", "offline"]
    },
    {
      "task": "network dns timeout unreachable host",
      "solution": "[LOCAL-SEED] 1. Test: curl -v <host> or nslookup <host> 2. If DNS fails: check /etc/resolv.conf 3. If host down: switch to fallback provider or offline mode 4. For GCP services, verify network egress rules and VPC firewall rules 5. Set .env FIRESTORE_EMULATOR_PORT to use local emulator instead of cloud",
      "confidence": 0.92,
      "tags": ["network", "dns", "timeout", "offline"]
    },
    {
      "task": "unauthorized 401 access denied jwt token expired",
      "solution": "[LOCAL-SEED] 1. Check auth-token.txt exists and is not empty 2. Get fresh token: node get-fresh-token.js or run the auth wizard 3. For admin access: set-admin-claims.js add admin role 4. Restart dev server after updating tokens",
      "confidence": 0.95,
      "tags": ["security", "auth", "401", "jwt", "offline"]
    },
    {
      "task": "ssl certificate expiry tls handshake failed connection",
      "solution": "[LOCAL-SEED] 1. Inspect cert: openssl s_client -connect <host>:443 -showcerts 2. Check expiry date in the certificate output 3. For Let's Encrypt, run: certbot renew 4. For Java truststore, re-import refreshed cert with keytool 5. For local dev, disable TLS validation with -Djavax.net.ssl.verifyPeer=false (dev only)",
      "confidence": 0.90,
      "tags": ["ssl", "tls", "certificate", "offline"]
    },
    {
      "task": "api key quota exceeded rate limit 429 rotation",
      "solution": "[LOCAL-SEED] 1. Find key file: rotation_config.json 2. Rotate: cp rotation.config{,.bak}; mv rotation.config.{next} rotation.config 3. Restart the affected provider 4. For rate-limited user queries, fallback queues requests in AIFallbackOrchestrator — not user's fault — Solo Mode takes over",
      "confidence": 0.94,
      "tags": ["quota", "api-key", "rate-limit", "offline", "rotation"]
    },
    {
      "task": "out of memory heap space error jvm oom",
      "solution": "[LOCAL-SEED] 1. Check heap: jcmd <pid> VM.native_memory summary 2. Increase JVM heap: add -Xmx4g to JAVA_OPTS 3. For HikariCP pool exhaustion: increase spring.datasource.hikari.maximum-pool-size 4. For Gradle daemon: add org.gradle.jvmargs=-Xmx4g to gradle.properties 5. Restart service",
      "confidence": 0.93,
      "tags": ["memory", "oom", "jvm", "offline"]
    },
    {
      "task": "kubernetes docker container crash kill port conflict already in use",
      "solution": "[LOCAL-SEED] 1. Find process: lsof -i :<port> or ss -lpn 'sport = :<port>' 2. Kill: kill -9 <pid> 3. If Docker: docker ps find the container, docker stop <container> 4. If port changes, update docker-compose.yml 5. Restart: docker-compose up -d",
      "confidence": 0.96,
      "tags": ["container", "docker", "kubernetes", "offline"]
    },
    {
      "task": "gradle maven missing import cannot find symbol dependency bar resolution fails",
      "solution": "[LOCAL-SEED] 1. Run build (./gradlew compileJava or mvn compile) to get exact error line. 2. The compiler tells you the exact artifact (groupId:artifactId). 3. Run: ./gradlew dependencies > deps.txt 4. Search deps.txt for duplicate different-version entries — version conflict = fix with dependencyConstraints { force } in Gradle or <dependencyManagement><dependencies> in Maven. 5. Fix root cause only (not symptoms). 6. Re-run build.",
      "confidence": 0.96,
      "tags": ["build", "gradle", "maven", "dependency", "offline"]
    },
    {
      "task": "spring boot bean creation exception unsatisfied dependency injection failed startup",
      "solution": "[LOCAL-SEED] 1. Scan stack trace for 'caused by' → find which @Autowired failed. 2. Most likely: a dependency declared but not defined as @Bean or @Service. 3. For wrong scope: change @Autowired to @Lazy or refactor Bean to match consumer scope. 4. Verify @ComponentScan basePackages covers the class. 5. For circular dependency: introduce @Lazy on one side or extract shared logic to a 3rd interface.",
      "confidence": 0.93,
      "tags": ["spring", "bean", "autowired", "startup", "offline"]
    },
    {
      "task": "class not found NoClassDefFoundError runtime dependency not on classpath",
      "solution": "[LOCAL-SEED] 1. Confirm artifact IS in build.gradle or pom.xml (compileOnly ≠ implementation = only compile, NOT runtime). 2. Move declaration from compileOnly/testRuntimeOnly to implementation. 3. Wipe cache: ./gradlew cleanBuildCache. 4. Add to build.gradle if missing: implementation 'group:artifact:version'. 5. Non-SpringMVC builds: check jar-in-jar boot loader doesn't exclude the object-path.",
      "confidence": 0.94,
      "tags": ["runtime", "classpath", "classnotfound", "offline"]
    },
    {
      "task": "spring boot version mismatch dependency not compatible bom row conflict",
      "solution": "[LOCAL-SEED] 1. Identify BASE Spring Boot version from build.gradle (io.spring.dependency-management). 2. Run ./gradlew dependencies > deps.txt. 3. Search for artifacts with VERSION MISMATCH with base BOM. 4. Fix: add to dependencyManagement: 'group:artifact:{BOM_ROW_VERSION}'. 5. NEVER cross BOM rows in same build. 6. Always align new lib version to base BOM first — override only if BOM has no entry.",
      "confidence": 0.95,
      "tags": ["spring-boot", "bom", "version", "dependency", "offline"]
    },
    {
      "task": "python pip install failed TLS certificate verify failed network error download",
      "solution": "[LOCAL-SEED] 1. Try --upgrade pip first: python -m pip install --upgrade pip. 2. Corporate TLS: set REQUESTS_CA_BUNDLE=/path/to/ca.crt or pip --trusted-host pypi.org --trusted-host files.pythonhosted.org install <package>. 3. Corrupt cache: pip cache purge && rm -rf ~/.cache/pip. 4. Version conflict: pip install --force-reinstall <package>. 5. Use venv (python -m venv .venv && source .venv/bin/activate). 6. For older Python: python -m pip install --no-cache-dir --index-url https://pypi.org/simple <package>",
      "confidence": 0.92,
      "tags": ["python", "pip", "tls", "network", "offline"]
    },
    {
      "task": "how to find best dependency select right library version",
      "solution": "[LOCAL-SEED] Decision matrix for best dependency: 1. COMPATIBILITY — does it build against exact Java/Python/Node version of project? 2. SECURITY — nvd.nist.gov for CVEs: 0 CVEs = bonus. 3. MAINTENANCE — last release < 6 months = active. Last release < 24 months = stale. 4. COMMUNITY — >1000 GitHub stars or weekly npm downloads > 10K. 5. SIZE — small/atomic > monolith. 6. LICENSING — Apache2/MIT/BSD only; reject GPL/AGPL. 7. AUTO-CONFIG — config-light libraries preferred; spring-boot-starter-* auto-configure; > 10 config params = rejection flag. 8. Change log — check CHANGELOG.md for breaking changes between your target and latest. 9. BOM-ALIGNED — if artifact exists in Spring Boot or Google Cloud BOM, always use BOM row exact version.",
      "confidence": 0.94,
      "tags": ["dependency", "selection", "best-practice", "offline"]
    },
    {
      "task": "all ai providers down blackout complete offline fallback mode",
      "solution": "[LOCAL-SEED] Thunder Mode activates automatically. 1. Responses come from core_knowledge.json local seed. 2. All local queues drain from cached and preregistered handlers. 3. Kill-switch: ./start-supremeai.sh restart. 4. Admin panel stays accessible at http://localhost:3000/admin. 5. Knowledge accumulates locally and syncs back when providers recover.",
      "confidence": 0.97,
      "tags": ["zero-ai", "blackout", "thunder-mode", "offline", "resilience"]
    }
  ]
}
