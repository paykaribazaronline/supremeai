{
  "name": "supremeai-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "dashboard",
    "functions",
    "tests",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "prepare": "node -e \"if(process.env.CI !== 'true') { try { require('husky') } catch (e) { process.exit(0) } process.exit(1) }\" || husky",
    "release": "standard-version",
    "format:root": "prettier --write \"**/*.{json,md,yml}\"",
    "lint:functions": "npm run lint -w functions -- --fix"
  },
  "lint-staged": {
    "dashboard/src/**/*.{ts,tsx}": [
      "npm run lint -w dashboard -- --fix"
    ],
    "src/main/java/**/*.java": [
      "./gradlew spotlessApply"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "standard-version": "^9.5.0",
    "turbo": "latest",
    "typescript": "~5.3.3"
  }
}
