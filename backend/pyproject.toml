[tool.poetry]
name = "supremeai-backend"
version = "2.0.0"
description = "SupremeAI 2.0 Backend - Autonomous AI Agent Platform"
authors = ["Your Name <your.email@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastapi = {extras = ["all"], version = "^0.136.0"}
uvicorn = {extras = ["standard"], version = "^0.51.0"}
starlette-context = "^0.3"
sqlalchemy = "^2.0.36"
alembic = "^1.14.0"
psycopg2-binary = "^2.9.10"
pydantic = "^2.10.0"
pydantic-settings = "^2.6.1"
loguru = "^0.7.3"
psutil = "^6.1.0"
redis = {extras = ["hiredis"], version = "^5.2.0"}
python-multipart = "^0.0.20"
passlib = {extras = ["bcrypt"], version = "^1.7.4"}
python-jose = {extras = ["cryptography"], version = "^3.3.0"}
# python-jose deprecated; migrated to PyJWT
# PyJWT = "2.10+" # added below when migration complete
python-dotenv = "^1.0.1"
requests = "^2.32.3"
uuid6 = "^2025.0.1"
boto3 = "^1.41.5"
defusedxml = "^0.7.1"
beautifulsoup4 = "^4.15.0"
posthog = "^7.29.0"
mcp = "^1.28.1"
stripe = "^15.3.1"
neo4j = "^6.2.0"
aiohttp = "^3.10.10"
websockets = "^13.1"
pygithub = "^2.5.0"
pydantic-extra-types = "^2.11.1"
supabase = "^2.11.0"
firebase-admin = "^6.5.0"
qdrant-client = "^1.12.1"
numpy = "^1.26.4"
opencv-python-headless = "^4.10.0"  # headless: no GTK/GUI libs needed in CI containers
pandas = "^2.2.3"
pillow = "^11.0.0"
cryptography = "^44.0.1"
pybreaker = "^1.4.1"
pytz = "^2024.2"
python-dateutil = "^2.9.0.post0"
email-validator = "^2.2.0"
asyncpg = "^0.30.0"
aiosqlite = "^0.20.0"
google-cloud-firestore = "^2.19.0"
google-cloud-storage = "^2.18.2"
google-auth = "^2.36.0"
google-auth-oauthlib = "^1.2.1"
google-auth-httplib2 = "^0.2.1"
openai = ">=1.54.0"
anthropic = "^0.120.0"
infisical-python = "^2.3.5"
litellm = ">=1.50.0,<2.0.0"
sse-starlette = "^2.1.3"
plotly = "^5.24.1"
scipy = "^1.14.1"
opentelemetry-sdk = "^1.28.2"
opentelemetry-api = "^1.28.2"
opentelemetry-instrumentation-fastapi = "^0.49b1"
opentelemetry-exporter-otlp-proto-grpc = "^1.28.2"

# বাংলা মন্তব্য: torch/sentence-transformers আগে মূল [tool.poetry.dependencies]-এ ছিল,
# ফলে CI-এর backend-core জব `poetry install --with dev` চালালেই torch-এর সাথে
# nvidia-nccl-cu12 *এবং* cu13 দুটো ভিন্ন CUDA স্ট্যাক ডাউনলোড হয়ে যেত -- CPU-only
# GitHub-hosted রানারে সম্পূর্ণ অপ্রয়োজনীয়, এবং এটাই ডিস্ক ফুরিয়ে দিয়ে
# `poetry install`/audit-sandbox ফেইল করাচ্ছিল (Zero-Operating-Cost নীতির বিপরীত)।
# grep দিয়ে যাচাই করা হয়েছে: torch-নির্ভর evolution/* মডিউলগুলো কোথাও import করা হয় না
# (dead/scaffold code, memory-note অনুযায়ী), আর sentence-transformers-এর ৪টা caller
# (experience_db.py, memory_service.py, knowledge_extractor.py, error_remediation.py)
# আগে থেকেই try/except ImportError দিয়ে গ্রেসফুলি ফলব্যাক করে -- তাই এই গ্রুপ বাদ
# দিলে backend-core-এর কোনো টেস্ট ভাঙে না।
# পরবর্তী ধাপ (এই sandbox-এ download.pytorch.org অ্যাক্সেস না থাকায় এখানে verify করা
# যায়নি বলে এখনই যোগ করা হয়নি): কেউ সত্যিকার এই গ্রুপ ইনস্টল করলে যেন GPU wheel না
# নামে, তার জন্য torch-কে explicit `https://download.pytorch.org/whl/cpu` source-এ
# পিন করে `poetry lock` রিজেনারেট করা।
[tool.poetry.group.ml]
optional = true

[tool.poetry.group.ml.dependencies]
torch = "^2.5.0"
sentence-transformers = "^3.3.0"

[tool.poetry.group.dev.dependencies]
# বাংলা মন্তব্য: pre-commit ও CI শুধুমাত্র ruff (linter + formatter) এবং mypy চালায় --
# black/isort এখানে ছিল কিন্তু কোথাও invoke হতো না (dead dev dependency), সরানো হলো।
ruff = "^0.4.8"
mypy = "^1.11"
pytest = "^8.0"
pytest-asyncio = "^0.23"
pytest-cov = "^5.0"
pytest-mock = "^3.14.0"
pytest-timeout = "^2.3.0"
pytest-xdist = "^3.6.1"
respx = "^0.21.1"
pytest-md = "^0.2.0"
typeguard = "^4.2"
playwright = "^1.49.0"
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.coverage.run]
# বাংলা: core/ ফোল্ডারেই coverage মাপা হয় — CI এর সাথে consistent।
# পুরো backend/ স্ক্যান করলে pytest অনেক ধীর হয়ে যায়।
source = ["core"]
omit = [
    "*/tests/*",
    "*/venv/*",
    "*/env/*",
    ".venv/*",
    "*/__pycache__/*",
    "*/migrations/*",
    "*/alembic/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["tests"]
# বাংলা মন্তব্য: লোকাল রানে coverage threshold-এর কারণে টেস্ট "পাস" সত্ত্বেও ফেইল করার ঝুঁকি
# এড়াতে --cov-fail-under সরানো হলো; CI নিজেই threshold এনফোর্স করে (supreme-core-ci.yml)।
addopts = "-ra -v --tb=short --strict-markers --strict-config --import-mode=importlib --cov=core --cov-report=term-missing --cov-report=html --cov-report=xml"
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "slow: marks tests as slow",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "chaos: marks tests requiring chaos engine",
    "requires_network: marks tests requiring network",
    "requires_redis: marks tests requiring redis",
    "e2e: marks tests as end-to-end tests",
    "security: marks tests as security tests",
    "performance: marks tests as performance tests"
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
    "ignore::UserWarning",
    "ignore::PendingDeprecationWarning",
]
asyncio_mode = "auto"
# বাংলা মন্তব্য: লোকাল রানেও CI-এর মতো ১২০ সেকেন্ড গ্লোবাল টাইমআউট নিশ্চিত করে।
# এর অভাবেই একটি টেস্ট ডেডলক করলে পুরো সুইট হ্যাং করত (রিপোর্টের "৩০% পরে হ্যাং")।
# thread মেথড pytest-asyncio auto মোডে async ও sync উভয় টেস্টের জন্য কাজ করে।
timeout = 120
timeout_method = "thread"

[tool.mypy]
python_version = "3.11"
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = true
disallow_untyped_defs = true
disallow_untyped_decorators = true
check_untyped_defs = true
ignore_missing_imports = true
plugins = [
    "pydantic.mypy"
]

[tool.black]
line-length = 120
target-version = ['py311']

[tool.isort]
profile = "black"
multi_line_output = 3

# বাংলা মন্তব্য: pylint এর disable তালিকা আগে 80+ রুল ছিল (অনেক W07xx ডুপ্লিকেট),
# এখন শুধু প্রয়োজনীয় রুলগুলো রাখা হয়েছে — বাকি সব সরানো হয়েছে
[tool.pylint]
max-line-length = 120
disable = [
    "C0301",  # Line too long
    "W0613",  # Unused argument
    "R0903",  # Too few public methods
    "R0902",  # Too many instance attributes
    "R0913",  # Too many arguments
    "C0415",  # Import outside toplevel
    "W0212",  # Access to protected member
    "W0622",  # Redefining built-in
    "R0914",  # Too many local variables
    "R0801",  # Similar lines in files
    "C0103",  # Invalid variable name
    "W0703",  # Catching too general exception
    "R0912",  # Too many branches
    "R0915",  # Too many statements
    "R0911",  # Too many return statements
    "R1702",  # Too many nested blocks
    "W0621",  # Redefining name from outer scope
    "C0114",  # Missing module docstring
    "C0115",  # Missing class docstring
    "C0116",  # Missing function docstring
    "W0237",  # Arguments renamed in subclass
    "W0221",  # Arguments differ from overridden method
    "W0236",  # Removed incompatibility
    "R1705",  # Unnecessary "else" after "return"
    "C0209",  # Formatting with % or .format
    "W0102",  # Dangerous default value arg
    "W0107",  # Unnecessary pass
    "C0411",  # Wrong import order
    "W0612",  # Unused variable
    "R1725",  # super() call style
    "W0404",  # Reimported
    "R0205",  # Class inherits from object
    "C0104",  # Disallowed name
    "W0707",  # Re-raising via 'raise ... from ...'
    "R1710",  # Inconsistent return
    "R1711",  # Useless return
    "C2801",  # Unnecessary dunder call
    "R0402",  # from import recommended
    "C0303",  # Trailing whitespace
    "C0304",  # Final newline
    "C0413",  # Wrong import position
    "R0916",  # Too many boolean exprs
    "W0631",  # Undefined loop variable
]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # Pyflakes — undefined names, unused imports
    "UP",    # pyupgrade — modernize Python syntax
    "B",     # flake8-bugbear — common bug patterns
    "I",     # isort — import ordering
    "N",     # pep8 naming conventions
    "S",     # flake8-bandit — security issues (hardcoded passwords, subprocess, etc.)
    "RUF",   # Ruff-specific rules
]
ignore = [
    # বাংলা: নিচের নিয়মগুলো প্রজেক্টের বাংলা কমেন্ট ও বিদ্যমান কোড স্টাইলের জন্য বাদ দেওয়া হয়েছে।
    "E501",     # Line too long — 120 char limit already set above
    "E402",     # Module level import not at top — main.py ENV bootstrap needs this
    "BLE001",   # Blind exception catch — addressed individually in health checks
    "RUF012",   # ClassVar mutable attribute — Pydantic models use this pattern
    "RUF001",   # Ambiguous unicode — বাংলা comments need this
    "RUF002",   # Ambiguous unicode docstrings — বাংলা docstrings
    "RUF003",   # Ambiguous unicode comments — বাংলা comments
    "RUF006",   # asyncio.create_task without storing ref — intentional fire-and-forget pattern
    "TRY002",   # Create own exception — addressed via core/exceptions.py
    "TRY004",   # Prefer TypeError
    "DTZ005",   # Datetime tz — handled by explicit UTC usage
    "B005",     # strip() with multi-char string — intentional in datetime parsing
    "B008",     # Default args call — FastAPI Depends() pattern requires this
    "B017",     # pytest.raises(Exception) — broad catch acceptable in these test cases
    "B904",     # raise from err — HTTPException chaining verbose without benefit
    "I001",     # Unsorted imports — handled by isort in pre-commit
    "N803",     # Argument name should be lowercase — some SDK callbacks
    "N805",     # First arg named 'self' — adapter_self pattern needed to avoid shadowing
    "N806",     # Variable in function should be lowercase
    "N811",     # Constant imported as non-constant — TTS alias is intentional
    "N812",     # Lowercase imported as non-lowercase — EC alias (Selenium convention)
    "N813",     # CamelCase imported as lowercase — circuit_breaker alias intentional
    "N815",     # mixedCase in class scope — Pydantic models use camelCase for JSON API
    "N817",     # CamelCase imported as acronym — ET alias is universal Python convention
    "N802",     # Function name should be lowercase — AST visitor methods (visit_FunctionDef, etc.) require PascalCase
    "N818",     # Exception not named with Error suffix — existing class names
    "S101",     # Use of assert — needed in tests and guards
    "S105",     # Hardcoded password string — false positives on param names
    "S106",     # Hardcoded password func arg — false positives
    "S113",     # requests without timeout — legacy code, tracked separately
    "S301",     # pickle.load — ML checkpoint loading, only from trusted paths
    "S324",     # hashlib.md5 — used for non-security IDs (short hash keys), not passwords
    "S603",     # subprocess — deployment scripts need this
    "S607",     # Partial executable path — deployment scripts
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S", "N"]   # test files: security + naming rules শিথিল
"**/migrations/**" = ["ALL"]   # alembic migrations: সব rules বন্ধ
