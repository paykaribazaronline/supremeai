# 1. Ignore EVERYTHING (including all hidden files/folders) by default
*
.*

# 2. ONLY ALLOW (Whitelist) the exact JAR file needed for deployment
!build/
build/*
!build/libs/
build/libs/*
!build/libs/app.jar