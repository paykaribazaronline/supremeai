services:
  - type: web
    name: supremeai
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    envVars:
      - key: PORT
        value: "8080"
    healthCheckPath: /api/health
    autoDeploy: true
